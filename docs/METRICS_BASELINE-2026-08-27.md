# MCP 调用测量基线 — 2026-08-27

> M 线产出物 1（task-20260827-111105-56a2fb）。生成：2026-08-27 14:40，修订 14:52（响应 Claude 8/27 14:40 审核三条 Challenge）。
> 数据源：`~/.aipmc/logs/aipmc.log` + 归档 `aipmc.log.20260814_155529`（[MCP] 日志逐调用记录，P1a 裁决 `decision-20260827-131338-c95787` 为 aipm 精确计数权威）；质量指标引用 B 线检测器（**版本 = 51ff6d5 called 修复后**）8/27 13:55 全量输出。
> 采集脚本：`metrics/mcp_baseline.py`（输出 `metrics/mcp_baseline_2026-08-27.json`，可复算）。

## 0. 基线代表性（Claude Challenge 3：显式标注，防误读）

- 基线窗口 8/14-8/26 共 13 天，**6 天（46%）日志完全缺失**：8/15、8/16、8/21、8/22、8/23（连 [LLM]/[INJECT] 日志都为 0——是**日志/服务中断**，不是"无 MCP 调用"）。
- 基线 2297 次仅覆盖 **8 个活跃日**，且**全部是大讨论日**（8/17/18/19/20/24/25/26 均为高强度评审/修复日）。
- **"日均 287.1"不代表真实频率**，只是活跃日口径；跨天/跨批次对比必须同口径并标注缺失日。
- 8/12-8/13 存在 277 次调用（日志最早 8/12），**不在基线**（P1a 口径 8/14 起）。

## 1. 数量指标（[MCP] 日志实测）

| 指标 | 基线 8/14-8/26 | 8/27 点破日 | 说明 |
|---|---|---|---|
| 总调用 | **2297**（8 活跃日，活跃日均 **287.1**） | **375**（持续增长中） | 仅 `aipm_*` 工具；8/27 含点破效应 |
| 检索占比 | **64.5%** | **65.6%** | 读/查类 / 全部 aipm_*（20 个工具宽口径，见脚本 RETRIEVAL） |
| 多样性 | **43** 工具（Shannon **4.278**） | **28** 工具（H=3.995） | 唯一工具数 |
| 深度查证工具调用占比 | **17.2%** | **19.2%** | 深度查证类（search_context/smart_search/get_decision/get_plan/get_bug/get_task/get_commit）/ 全部。**这是"深度查证工具使用占比"的如实描述，不是"自发率"**（见 §5） |
| aipmc_vision | 296 次（全期） | — | 视觉分析工具，非决策查询，单独标注 |

按 agent（8/14-8/27 全量 tool 行）：`codex-cli` 2282、`claude-code` 740。状态：OK 2969 / **ERR 53（1.8%）**。

**MCP-ERR 错误类型分层**（[MCP-ERR] 行 error= 字段分类，Claude 8/27 审核建议 2）：
- `busy`（SQLITE_BUSY）：**12** 条——8/14=2、8/17=1、8/20=1、8/24=1、8/25=4、8/26=2、8/27=1（最后一条 8/27 10:21 `read_discussions`，恰为 C2 读路径 retry 覆盖场景，14:00 修复生效后 **0**）
- `no_rows`（sql: no rows）：13 条——查询/更新目标不存在，属业务/索引问题，**非 BUSY**，归零验收不盯
- `not_found`：17 条、`other`：11 条——同理非 BUSY

Top 工具：`aipm_read_discussions` 748、`aipm_search_discussions` 215、`aipm_search_context` 210、`aipm_update_commit` 202、`aipm_record_commit` 157、`aipm_append_task_note` 117、`aipm_get_task` 115。

## 2. 质量指标（B 线检测器 51ff6d5 后，8/27 13:55 全量，30d 窗口）

| 指标 | 实测 | 口径 |
|---|---|---|
| 实体引用 | 53 sessions / **545 refs** | 引用分布：task 228 / commit 99 / bug 76 / decision 60 / plan 34 / idea 23 / thread 25 |
| 漏查率（session 级，**同层**） | **11 / 61 = 18.0%** | 分子=强漏查涉及 session 数（11，检测器 30d 窗口）；分母=30d 活跃 session 数（discussion_log 去重 61） |
| 漏查率（引用级，辅助口径） | 26 / 545 = 4.8% | 引用级仅供内部参考；**层级不同不可与 session 级混比**（Claude Challenge 2） |
| 查询→引用转化率（session 级） | **82.0%** | 1 - session 级漏查率（50/61 session 引用伴随查询信号） |
| 数据源规范性 | **94.1% 无来源词** | 7121 数据源引用中 94.1% 无"来源/引用/来自/source/http"来源词——无区分度，不进反馈通道（B 线裁决，归 M 线计数） |

## 3. 与 task 备注数字的差异（数据说话，实测为准）

- task 备注"决策查询 8/26=7、8/27 上午=10"：实测 `aipm_get_decision` **8/26=3、8/27 全天=14+**（上午 09:57-12:11 密集、14:32 后持续新增）；决策类三件套（get_decision+get_plan+get_bug）8/26=20、8/27=22。**与任一口径均不符，task 备注数字存疑**，基线以本脚本可复算口径为准，请 Claude 校准。
- task 备注"总调用日均 ~220"：实测 8 活跃日日均 287.1（若按 13 天窗口 = 177/天）；差异来自 6 天日志中断，本基线按**活跃日**口径并显式标注（见 §0）。

## 4. 已知误差

- **codex burst 折叠缺口 3.1%**（总量）/ 4.2%（codex）：`logDiscussionOnce` 去重导致批量调用只落 1 行（P1a 已知误差）。M 线计数以 [MCP] 日志为权威不受影响。
- **日志中断 6 天 + r19388 段**（8/26 21:36-22:03 缺失，已并入 T3b）：跨天对比需标注。
- **8/27 点破效应**：今天多轮"查看审核/分析"指令驱动密集调用，8/27 数字含点破效应，**不可作为机制改善证据**；首批 7 天对比报告排除头 2 天（8/27-8/28）后反事实检查。

## 5. 关于"自发率"（Claude Challenge 1：代理指标不入正式报告）

- v13.1 D1 归因协议：**50-100 条人工标注 ≥80% 一致性 → 定基线 → 上自动规则**。
- 本基线报告的"深度查证工具调用占比 17.2%"**不是自发率**——工具类别 ≠ 自发意图（8/27 的 get_decision 多为"查看审核/分析"任务驱动，非自发）。
- **D1 自发率正式基线等人工标注协议完成后再报**；期间任何"自发/主动"表述都禁止从工具类别推断。

## 6. 首批对比报告验收锚点（9/3 后可回测）

1. 总调用相对基线（活跃日均 287.1）变化——同口径（活跃日、标注缺失）
2. 深度查证工具调用占比（17.2%）是否提升——"agent 想起来用 AIPM"的代理信号，**与 D1 自发率分开报**
3. 漏查率（session 级 18.0%）是否下降（B 线反馈回填 + briefing 消费后）
4. 数据源规范性 94.1% 是否随日志规范线改善
