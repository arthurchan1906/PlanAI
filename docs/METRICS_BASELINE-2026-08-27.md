# MCP 调用测量基线 — 2026-08-27

> M 线产出物 1（task-20260827-111105-56a2fb）。生成：2026-08-27 14:40。
> 数据源：`~/.aipmc/logs/aipmc.log` + 归档 `aipmc.log.20260814_155529`（[MCP] 日志逐调用记录，P1a 裁决 `decision-20260827-131338-c95787` 为 aipm 精确计数权威）；质量指标引用 B 线检测器 8/27 13:55 全量输出。
> 采集脚本：`metrics/mcp_baseline.py`（输出 `metrics/mcp_baseline_2026-08-27.json`，可复算）。

## 1. 基线窗口与口径

- 基线 = **8/14-8/26**（P1a 口径「8/14 起」）；**8/27 点破日单独列**（今天用户点破"metrics 衡量 MCP 使用率"，首批对比报告须排除点破效应头 2 天）。
- 数量指标只统计 `[MCP] tool=...` 行且工具为 `aipm_*`（排除 `test_tool` 41 次、`aipmc_vision` 单独标注）。
- 8/12-8/13 存在 277 次调用（日志最早 8/12），**不在基线**（P1a 口径 8/14 起）。
- **日志缺口日 6 天**：8/15、8/16、8/21、8/22、8/23 连 [LLM]/[INJECT] 日志都为 0（无任何活动记录）——是日志缺失而非"MCP 未调用"。基线 13 天中仅 **8 个活跃日**。

## 2. 数量指标（[MCP] 日志实测）

| 指标 | 基线 8/14-8/26 | 8/27 点破日 | 说明 |
|---|---|---|---|
| 总调用 | **2297**（8 活跃日，日均 **287.1**） | **367** | 仅 `aipm_*` 工具 |
| 检索占比 | **64.5%** | **65.4%** | 读/查类 / 全部 aipm_*（分类见脚本 RETRIEVAL） |
| 多样性 | **43** 工具（Shannon **4.278**） | **28** 工具（H=3.987） | 唯一工具数 |
| 自发率 | **17.2%** | **19.4%** | 深度查证类（search_context/smart_search/get_decision/get_plan/get_bug/get_task/get_commit）/ 全部（口径 v1，见备注） |
| aipmc_vision | 296 次（全期） | — | 视觉分析工具，非决策查询，单独标注 |

按 agent（8/14-8/27 全量 tool 行）：`codex-cli` 2245、`claude-code` 737。状态：OK 2929 / **ERR 53（1.8%）**。

Top 工具：`aipm_read_discussions` 746、`aipm_search_discussions` 215、`aipm_search_context` 209、`aipm_update_commit` 201、`aipm_record_commit` 157、`aipm_append_task_note` 116、`aipm_get_task` 115、`aipm_create_task` 100、`aipm_update_task_status` 95。

## 3. 质量指标（B 线检测器 8/27 13:55 全量，30d 窗口）

| 指标 | 实测 | 口径 |
|---|---|---|
| 实体引用 | 53 sessions / **545 refs** | 引用分布：task 228 / commit 99 / bug 76 / decision 60 / plan 34 / idea 23 / thread 25 |
| 漏查率 | **26 / 545 = 4.8%** | 强漏查（引用决策/计划/缺陷但未调用对应查询工具），已回填反馈（4 session / 13 refs） |
| 查询→引用转化率 | **95.2%** | 1 - 漏查率（545 次引用中 519 次伴随查询信号） |
| 数据源规范性 | **94.1% 无来源词** | 7121 数据源引用中 94.1% 无"来源/引用/来自/source/http"来源词——无区分度，不进反馈通道（B 线裁决，归 M 线计数） |

## 4. 与 task 备注数字的差异（数据说话，实测为准）

- task 备注"决策查询 8/26=7、8/27 上午=10"：实测 `aipm_get_decision` **8/26=3、8/27 全天=14**（上午 09:57-12:11 共 12 次、14:32 后 2 次）；决策类三件套（get_decision+get_plan+get_bug）8/26=20、8/27=22。**与任一口径均不符，task 备注数字存疑**，基线以本脚本可复算口径为准，请 Claude 校准。
- task 备注"总调用日均 ~220"：实测 8 活跃日日均 287.1（若按 13 天窗口 = 177/天）；口径差异来自 6 天日志缺口，本基线按**活跃日**日均并标注缺口。

## 5. 已知误差与反事实注意

- **codex burst 折叠缺口 3.1%**（总量）/ 4.2%（codex）：`logDiscussionOnce` 对连续相同 assistant 内容去重，批量调用只落 1 行（P1a 裁决标注为已知误差，T3b 修复后回测归零）。M 线计数以 [MCP] 日志为权威不受影响。
- **日志缺口 6 天**：8/15/16/21/22/23 无日志（含 r19388 段 8/26 21:36-22:03 缺失，已并入 T3b）。基线日均按活跃日，跨天对比报告需标注。
- **8/27 点破效应**：今天用户多轮"查看Claude审核/分析"驱动密集调用，8/27 数字（367 次）含点破效应，**不可作为机制改善证据**；首批 7 天对比报告排除头 2 天（8/27-8/28）后反事实检查。

## 6. 首批对比报告验收锚点（7 天后回测）

1. 总调用相对基线（日均 287.1）变化
2. 自发率（深度查证类占比 17.2%）是否提升——这是"agent 想起来用 AIPM"的核心信号
3. 漏查率（4.8%）是否下降（B 线反馈回填 + briefing 消费后）
4. 数据源规范性 94.1% 是否随日志规范线改善
