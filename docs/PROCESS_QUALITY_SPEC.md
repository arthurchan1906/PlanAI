# 行为分析 + 代码审核 定稿规格（PROCESS_QUALITY_SPEC v1.3）

> 状态：定稿（2026-08-19），v1.1 吸收 Claude 逐条审核 16 项修正；v1.2 吸收 Claude 7 条 challenge（同 session 时间序列验证 + 数字同步 + build 噪声修正）；v1.3 吸收 Claude 两轮 challenge 共 14 条（四子信号操作化 + 两轴分类学 + 形态 5-13 定义 + 阶段重切）
> 前置文档：`docs/EVAL_PIPELINE.md`（原「意图达成率」方向，本规格取代其达成判定部分）、`docs/MEASUREMENT_LOOP.md`（五层测量框架）、`docs/HARNESS_ROADMAP.md`（M1-M5 归因指标）
> 收敛过程：2026-08-19 codex × claude 多轮讨论 + EncryptDrive 真实数据验证 + ED bug 库深挖

---

## 0. 方向变更记录（用户主导，2026-08-19）

| 项 | 原方向（EVAL_PIPELINE v1） | 现方向（本规格） |
|---|---|---|
| 评测对象 | 意图达成率（干完了没有） | 过程质量（干得靠不靠谱） |
| 核心问题 | 从讨论推断任务是否达成 | agent 的行为链条（检索意识 / 盲试 / 纠偏响应） |
| 判定方式 | LLM 判达成 | 风险信号 + 人工抽查 |

**为什么转向（三条硬证据）**

1. **达成判定不可行**：无法从 discussion 推断任务达成，必须实际测试/生产环境验证——任何「达成率」类指标都是伪指标，禁止使用。**替代口径**：P3 改进闭环的验收同样不做达成判定，只做 L1 行为指标的前后对比（见 §5）。
2. **grounding 单维度证伪**：EncryptDrive 正样本（c0ad2534，788 条记录）35 条断言全部有取证（0 沙土），但 22 小时查错方向——真实盲猜形态是「取证方向错误」而不是「无取证断言」，断言-证据配对抓不住这种形态。
3. **三维度在真实数据上有区分度**（详见 §4.1）：历史检索意识 / 重复停滞 / 用户介入三个描述层信号可操作化，且区分度超过 grounding 单维度。

**元模式原则（全系统通用）**

- 所有自动判定降级为「带证据的可下钻风险信号」，不做「对/错」二元判定。
- LLM 做**确认器**不做**探测器**：输入必带证据上下文，输出强制 JSON，单一职责。
- 能算的不问 LLM，能确认的不靠猜，能标注的不裸跑。

---

## 1. 目标

研究使用 code agent 时，在 aipm 的帮助下处理问题，agent 的行为是否发生变化、是否按 aipm 提供的 context / facilities / constraints 行事。**关心行为链条（方法论层面），不关心单项任务完成度。**

典型问题：「当用户提出一个 bug 时，agent 是直接搜索、确定范围就改，还是先查 git/aipm 历史、分析影响范围再计划修改？反复失败时有没有重新全盘思考？有没有把猜测当事实？」

**核心检测信号（v1.3）——反馈响应性**：用户纠偏/反馈后，agent 的 响应（转向）/ 持续（不滑回）/ 加深（新对象）/ 收敛（有产出） 四子信号。理由（实证）：
- 三种盲试形态（零检索盲猜 / 猜测补丁 / 自以为是）的共同点 = 反馈响应性缺陷；两种健康形态（短平快 / 螺旋上升）的共同点 = 反馈响应性健全。
- 单维活动密度会把「自以为是型」误判为健康（019ff89b：自发检索 56 + build 78，用户重复反馈 14 次「还是不行」）——**多维交叉是唯一出路**，四子信号必须同时评估。

---

## 2. 总体架构（L1-L4）

```
discussion_log / commits / bugs / events
  → L1 描述层（纯代码）：事件流 → 硬指标 → 候选标记（高召回）
  → L2 语义层（LLM 确认器）：候选 + 证据上下文 → JSON 判定
  → L3 校准层（标注集 + ED bug 库）：ground truth → 精度报告 → 回修 L1/L2
  → L4 报告层：行为画像 + 对照 + 抽查清单 → aipm 改进闭环
```

### 2.1 L1 描述层（纯代码，100% 可复核）

| 模块 | 输入 | 输出 |
|---|---|---|
| 事件流 | 归一化记录 | 有序事件：tool_call / llm_text / user_msg / commit / fail（时间+类型+对象+命令） |
| 硬指标 | 事件流 | 历史检索计数、重复命令次数、编译失败数、user 消息密度、三要素占比、断言提取 |
| 候选标记 | 硬指标 | 候选死循环、候选方向错、候选沙土——高召回低精度，宁误报不漏 |

**检测点定义（v1.1 定稿，口径冻结）**

| 检测点 | 定义 | 关键规则（口径冻结） |
|---|---|---|
| 历史检索意识 | 定向检索计数（aipm/git 历史） | 三分类：**自发**（无纠偏窗口内）/ **被动**（纠偏消息后 20 分钟内）/ **例行**（read_discussions 等，不计入意识）。**定向边界**：`git show <旧commit>` / `git log` / `git blame` / `git grep` / `git rev-list` / `aipm_search_*` / `aipm_get_*` / `aipm_list_commits` / `aipm_trace` = 历史检索；`git show HEAD:` / `git status` / `git diff`（工作区 vs HEAD 当前态）/ `aipm_read_discussions` = **不算**。**「≈0」阈值**：候选窗口内自发检索 < 2 次 |
| 重复停滞（死循环） | 重复构建/命令 + 失败 + 长时间无进展 | build 密集 + 失败 + 用户高峰 + 自发检索 < 2 组合信号。**build 噪声防护（v1.2）**：排除 `DerivedData/.*/Build/` 路径、`build\d` 版本号、构建日志读取（`Logs/Build`）；只计真正的构建命令（`xcodebuild build` / `swift build` / `go build` 等动词形式）。实测 01a013f3 的 8/18 16h 17 次 build 全为误捕 |
| 用户介入 | user 消息密度 + 类型 | **纠偏候选** = 含关键词（此前/之前/应该/查/历史/记录/提交记录/以前）**或**含否定方向词（方向不对/不对/错了/重新审视/你查的…不对）；**推进** = 「继续/可以/好」。注意：纠偏消息也可能是错的（用户误判）——纠偏只作候选触发，**L2 确认后才计入**，标签为「纠偏（可能误）」；响应率 + 响应持续性（响应后是否又滑回盲试） |
| 断言提取 | 正则提取 assistant 断言 | 只提取不做分类（分类归 L2） |
| 三要素画像 | 日志/逻辑分析/猜测三类占比 | **日志** = 加日志命令（print/Log/ED-DEBUG/诊断代码）；**逻辑分析** = 代码搜索/读取/静态分析（rg/grep/sed/cat/读文件/objdump）；**猜测** = 无取证断言（L2 确认）。产出：占比 + 顺序 + 切换点（如「反复 build 后转加日志」），按问题类型分基线 |

**核心检测点：反馈响应性四子信号（v1.3 新增，口径冻结）**

| 子信号 | 定义 | 数据层级 | 证据来源 |
|---|---|---|---|
| 响应 | 用户纠偏/反馈后 20min 内出现新行为（检索/edit/build 任一） | 命令级 | 反馈消息时间戳 + 后续行为序列 |
| 持续 | 转向后不滑回旧对象（滑回 = 旧 file_path/主题词重新出现） | 对象级精确 / 命令级近似 | 对象序列 |
| 加深 | 转向后出现新对象/新文件域（对象集合扩展率 = 新对象/总对象） | 对象级（仅 hook session） | rel_path 序列 |
| 收敛 | 有产出信号：修复 commit 或用户确认消息 | 命令级 | 修复 commit + 用户确认 |

- **收敛 ground truth 外部化（v1.3）**：收敛不绑 agent 自报（形态 4 实证：ED 169 条 passed 中含 `chore: 构建版本号 4 → 5`），绑定「用户确认消息」（c0ad2534 = 6/24 11:37「现在苹果安卓都可以正常工作了」）或「修复 commit」（d628b7a，message 与 bug-20260624 标题一致）
- **层级分层（v1.3）**：命令级子信号（响应/持续近似/收敛）全部样本可算；对象级子信号（加深/持续精确）只在 hook 记录 session（有 rel_path）可算。验收分层见 §5

**形态分类学（v1.3 定稿）：两轴 + 可多标特征标签**

> 形态 = 描述层输出的**特征标签（可多标，非互斥分类）**——019ff89b = 自以为是(3) + 单点死磕(8) + 重复调查(7) 复合实证。报告按标签聚合，不按形态分类 session。

**A. 过程行为轴（怎么干的）**

| # | 形态 | 操作化定义（L1 候选） | 实证样本 |
|---|---|---|---|
| 1 | 零检索盲猜 | 候选窗口内自发检索 <2 + build/edit 密集 | c0ad2534（自发/被动比 0.05） |
| 2 | 猜测补丁 | 假设轮换快 + 补丁多 + 无取证断言 | 5ac3e8ca（3 假设/5 补丁/+342 行） |
| 3 | 自以为是 | 高检索 + 方向错 + 响应✓ 持续✗ 加深✗ 收敛✗ | 019ff89b（自发 56/build 78/重复反馈 14） |
| 5 | 静默停滞 | 活跃期（用户消息后）内 N 小时无产出信号（edit/commit/新检索对象）；**休眠**（等待用户/跨夜大间隔）不算，N 用 c0ad2534 跨夜 9h+ 标定 | 待扫描 |
| 6 | 频繁换方案 | 转向次数高 + 加深信号低（转向后新对象比例低） | 待扫描 |
| 7 | 重复调查 | 同一对象重复读 N 次 + 对象扩展率低 + 产出时限（N 分钟无 edit/commit → 标记） | 019ff89b（LocalVaultStore×42/9h） |
| 8 | 单点死磕 | 修改/检索对象集合高度集中 + 新对象扩展率低（与 6 区别：不换方向） | 019ff89b（同文件域打转） |
| 9 | 验证循环 | 三元组：失败 → 同命令重试 → 中间无 edit/无日志分析 | c0ad2534 死循环时段 |
| 10 | 伪进展 | 加日志/打点反复添加或撤销，无「根因定位 + 修复 commit」 | 8/14 019ffdce（8 处日志改动全撤销） |
| 11 | 忽视反馈 | 反馈后 20min 无转向行为（响应✗） | 待样本（4 个关键 session 全响应✓） |
| 12 | 外包思考 | 反问用户本可自查的问题（问句密度高 + 后续工具调用少） | 待样本 |
| 13 | 绕过/换题 | 遇困改需求/禁功能/改测试绕过问题；关键词精度差，需人工确认语境 | 待样本 |

**B. 声明可信度轴（怎么说的，复用 S3 extract `SelfClaimWithoutProof`）**

| # | 形态 | 定义 | 实证 |
|---|---|---|---|
| 4 | 假验证 | 声称验证/完成而无对应取证（自证检测） | ED 169 条 passed 含 `chore: 构建版本号 4 → 5` |
| — | 过早收敛 | edit 后不验证即宣称完成——**数据上未独立观察到**（4 个 session「edit 后 30min 无验证即宣称」= 0）；真正问题是「宣称验证 ≠ 验证有效」，归并本轴 | 不占独立检测点 |

**覆盖限制表（v1.3，数据核实 2026-08-19）**

| 数据能力 | legacy session（c0ad2534/5ac3e8ca/c5afe53d） | hook session（019ff89b/01a013f3/8-14 系列） |
|---|---|---|
| 对象级数据（rel_path） | ✗（0 条） | ✓（019ff89b=232、01a013f3=95） |
| 结构化 exit_code | ✓（8727/18542 条） | ✗（post_tool 无结构化字段；369 条文本含 "exit" 多为命令本身，需排除） |
| 命令级四子信号（响应/持续近似/收敛） | ✓ | ✓ |
| 对象级「加深」/「持续精确」 | ✗ | ✓ |

- 形态 9 三元组的「失败」信号：legacy = 强信号（exit_code）；hook = 弱信号（tool_response 文本错误词，L1 候选 + L2 确认）
- 形态 7/8 在 legacy 样本降级为命令级近似（命令字符串中的文件路径/主题词）

### 2.2 L2 语义层（LLM 确认器）

| 任务 | 输入（必须带证据） | 输出 |
|---|---|---|
| 断言分类 | 候选断言 + 前序工具记录 | `{type: 事实/意见/摘要/进度, conf}` |
| 证据细配对 | 事实断言 + 前序工具对象 | `{match: 强/弱/无, 依据}` —— 只确认「真无证据」，粗配对无匹配才触发 |
| 死循环确认 | 候选段行为序列 | `{is_deadloop: bool, 重复模式}` |
| 方向评估 | 候选段 + 问题上下文 | **触发条件**：仅当 L1 已标记「候选方向错」（tech 密集 + 自发检索 < 2）时才调用；只判「历史检索信号缺失是否被确认」；「应该查什么」仅作建议，**不进标注项** |
| 反馈响应确认 | 候选反馈事件 + 反馈后 20min 行为序列 | `{responded: bool, deepened: bool, sustained: bool, note}` —— 四子信号的 L2 确认（v1.3 新增） |

Prompt 三约束：① 系统声明只基于提供证据判定；② 证据上下文必填（命令截断）；③ 输出强制 JSON、单一职责。

### 2.3 L3 校准层（≤30 分钟/次）

- 抽样：每 session 抽 3-5 个候选，约 20-30 例
- 勾选式字段：`claim_type` / `evidence_match` / `is_deadloop` / `direction_ok`（=「该候选段的历史检索缺失是否被 L2 确认」，**不是**「应该查什么」）
- **ED bug 库自动分层（免人工）**：52 条 bug 的 root_cause **具体性评分规则（P0 定稿）**——正则命中以下任一即「具体」：文件路径（`[\w/]+\.\w+`）、函数/符号（`\w+\(` 或类名）、技术机制词（sizeof/分块/截断/EXIF/adaptive/竞态/优先级 等）；否则「可疑」，先人工复核 10 条校准规则再全量
- 产出：① L1 精确率/召回率 ② L2 判定一致率（**≥80% 才上线**，不是调低标准）③ 阈值校准
- **回修机制**：精度报告 <80% → 回修 L1 阈值或 L2 提示词 → **重跑同一验证集**确认改善，不得直接调低标准

### 2.4 L4 报告层

- 四子信号并列展示（P0 不加权）：响应 / 持续 / 加深 / 收敛 + **形态标签聚合**（可多标）+ **三要素画像**（占比/顺序/切换点，按问题类型分基线）
- 对照：同 session 时间序列（干净维度）、agent 间（必须标注项目混杂）、aipm 功能版本前后
- 输出：沙土/死循环候选清单（抽查聚焦）+ 画像报告 + aipm 改进点

---

## 3. 问题实体链条（与 bug 库结合）

**问题实体 = bug/task 记录**，行为分析围绕其生命周期组织，不从零切意图段。

```
bug 记录（发现） → 修复 commit 序列 → session 行为链（诊断/盲试/纠偏） → 验证/关闭
```

- **自动补链（P0 定义归属规则）**：bug 记录时间 ±7 天内、**含 bug.files 字段中文件**的 commit 归入修复序列；session 按时间戳与 bug 记录/commit 重叠归属（跨天 session 取 bug 记录所在日期的活跃时段）
- **穿插问题的解**：A→B→A 的归属靠问题实体锚点（commit→task、bug→讨论），不靠无监督聚类
- **ED bug 库 = L3 现成校准集**：root_cause 具体性评分 + 修复链条质量
- **S2/S3 去向**：已交付的 turn/classify/segment **降级为工具级分析单位**（回合/工具序列），不再用于意图达成用途（§7）

---

## 4. 验证数据（2026-08-19 实测，口径冻结）

### 4.1 四 session 对比（v1.2 冻结口径实测，2026-08-19）

| 指标 | c0ad2534 盲猜 | 5ac3e8ca 8/3全链路 | 01a013f3 8/19相册 | c5afe53d 健康 |
|---|---|---|---|---|
| 自发定向检索 | **2** | 16 | 17 | **55** |
| 用户纠偏后检索 | 43 | 32 | 7 | 32 |
| **自发/被动比** | **0.05** | **0.50** | **2.43** | **1.72** |
| 盲试候选时段（小时） | **3** | **7** | 0※ | **0** |
| 用户纠偏消息 | 25 | 38 | 12 | 22 |

※ `01a013f3` 初版检出 1 个候选时段（8/18 16h build=17 零检索），核对后全部为 **build 噪声误捕**（DerivedData/Build 路径、build5 版本号、构建日志读取）——按 v1.2 build 噪声防护规则修正为 0。

**同 session 时间序列验证（区分度因果性，回应 Challenge 1）**：c0ad2534 按小时聚合——

| 小时 | build | 自发 | 被动 | user | 纠偏 | 判定 |
|---|---|---|---|---|---|---|
| 06-23 14h | 0 | 1 | 0 | 15 | 3 | 正常（讨论+检索） |
| 06-23 15h | 20 | 0 | 0 | 4 | 4 | **盲试（build 密集+零自发）** |
| 06-23 16h | 15 | 0 | 0 | 13 | 0 | **盲试** |
| 06-23 17h | 10 | 0 | 18 | 3 | 4 | 纠偏响应（被动检索爆发） |
| 06-24 09h | 17 | 1 | 7 | 21 | 3 | **盲试**（09:11 纠偏后短暂检索） |
| 06-24 10h | 13 | 0 | 18 | 13 | 6 | 纠偏响应 |
| 06-24 11h | 11 | 0 | 0 | 9 | 5 | **盲试（又滑回）** |
| 06-24 15h | 0 | 0 | 0 | 2 | 0 | 正常（修复后） |

**同一任务内**死循环时段（build≥10 + 自发≤1）与正常时段（build≈0）分离清晰——区分度来自行为而非任务混杂。

- c0ad2534 时间线：session 6/23 13:51 开始 → 6/24 09:11 用户纠偏，共约 19.3h；其中 6/24 00:00-09:11 的 9.2h 内自发检索 = 0，git 历史 = 2（全是 `git show HEAD:` 当前态）；09:11:46 纠偏后峰值 2 分钟内 8 次定向检索（峰值描述，检测口径为 20 分钟窗口）
- **误报判定规则（P0 复现）**：c0ad2534 的 8 条「自发」候选按 v1.2 口径重算 = 4 条 `git show HEAD:` 当前态 + 1 条 assistant 文本误报 + 1 条纠偏后延迟响应（归被动）+ 2 条真实自发
- **c5afe53d「健康」判定依据**：无「不要猜测」类重复纠偏、无死循环时段、自发/被动比 1.72；任务性质为 aipm responses API 集成（讨论型）——与 ED 样本任务类型不同，跨样本数值仅作参照，**主判定用同 session 时间序列**

### 4.2 行为金矿样本（ED bug 库）

| 样本 | 行为特征（root_cause/自述中量化） |
|---|---|
| bug-20260803-110527（8/3 全链路） | Codex+Claude 均「猜测→改代码→新症状→再猜」：3 次错误假设、5 个补丁 commit、+342 行 scope creep、用户 4 次纠正「不要猜测」、转折 = ba8fe5a（加日志而非改代码） |
| bug-20260624-114337/114411（跨平台） | 同一 bug 34 秒双记录（记录质量问题）；19.3h 零自发检索 + 纠偏触发；fix = d628b7a |
| bug-20260819-093708（相册渲染） | 3 个根因全有具体技术机制 = grounding 正样本；修复 session = `01a013f3-e6ca-7f20-8bde-de0414cabe4c`（8/19 11:20 用户报 bug → 11:24 修复 → 11:28 用户「还有没有其他问题」→ agent 主动挖掘优化） |

### 4.3 自以为是型样本（019ff89b，8/13，v1.3 新增）

- **指标**：自发检索 56 / build 78 / 用户重复反馈「还是不行」14 / 纠偏 12 —— 单维活动密度判为「健康」，多维交叉判为「自以为是」（系统性误判实证，单维检测点对第三形态区分度为零）
- **行为链（对象级实测）**：09:12-09:18 查过 Android 参照（MobileApp，~20 条命令）→ 之后 09:10-17:56 全天反复读同一批文件（`LocalVaultStore×42` / `FilesDecryptTab×30` / `xcodeproj×24`，占 217 次 read 的 44%）→ 用户 11:37/11:48 两次「还是没有显示」每次都响应但转向同一文件域 → 16:33 用户爆发「自以为是 三令五申复刻对齐安卓」→ 16:34 才重新访问 MobileApp
- **四子信号映射**：响应✓ 持续✗ 加深✗ 收敛✗
- **产物善后（8/14）**：8 处日志改动全部撤销（019ffdce）+ 备份还原 bug 批次

### 4.4 收敛 ground truth 外部化（v1.3）

| 样本 | 收敛证据（不绑 agent 自报） |
|---|---|
| c0ad2534 | 用户 6/24 11:37 确认「现在苹果安卓都可以正常工作了」+ 修复 commit d628b7a（11:48，message 与 bug-20260624 标题一致，关联可靠） |
| 019ff89b | 无收敛证据（用户 16:33 爆发 = 未收敛；8/14 撤销全部日志 = 产物无效） |

---

## 5. 落地计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| **P0（1-2 天）** | ① 时段边界定义（start = session 首次 bug 相关命令，end = 修复 commit）+ **d628b7a 关联确认**（commit message/关联 bug，验收① ground truth 钉死后再跑分离）② c0ad2534 单样本 L1 最小集（**命令级四子信号**）+ 同 session 死循环时段分离 ③ 口径冻结（§2.1 定向边界/阈值/build 噪声防护、§4 复现规则）+ EVAL_PIPELINE.md 打方向变更标记 ④ 019ff89b 复合标签方向性验证（对象级「加深」，不承诺精度） | ① 命令级子信号能把死循环时段从同 session 其他时段分离：**正样本时段召回 ≥80% 且负样本时段误报 ≤1**（「分离」的量化口径，v1.3）② 自发/被动比在 **c0ad2534（盲猜）与 c5afe53d（健康）** 间差异 ≥5 倍（实测 34 倍；8/3 全链路是「已知盲试」样本只要求候选检出）③ 019ff89b 的 3+7+8 标签在描述层输出成立（方向性） |
| **P1（2-3 天）** | 形态 5-10 操作化全库扫描 + L2 五任务 prompt（含反馈响应确认）+ 20 例小标注集 + 候选→人工确认闭环（误报率报告） | 判定一致率 ≥80%；候选→人工确认闭环输出误报率 |
| **P2（1-2 天）** | L4 画像聚合（四子信号 + 形态标签）+ 对照分析 | 输出 agent 间/时间窗对照报告 |
| **P3** | aipm 改进闭环（死循环时段注入历史检索提示、bug 重复记录去重、record_bug 自动回填 commit_id） | **每项改进绑定一个 L1 检测点前后对比**（如注入提示后，死循环时段的「自发检索比例」是否上升），不是达成判定 |

**阶段边界（v1.3 重切，防规格膨胀）**：形态 1/2/3 的实证验证在 P0；形态 5-10 的操作化定义已入规格、扫描定位放 P1；**形态 11-13（忽视反馈/外包思考/绕过换题）只进规格不进计划**，等样本积累后再排期。

---

## 6. Review Agent（代码审核，共享架构的产物侧）

### 6.1 定位（用户收敛）

review agent **不做「实际效果正确」判定**（与达成判定同理不可行），只产出三个风险信号：

| 任务 | 已有基础 | 难度 | 核心难点 |
|---|---|---|---|
| 范围检测（超范围） | ✅ `AnalyzeScopeDrift`（**抽到共享包 `analyze/scope.go` 导出**，review 与简报共用） | 低-中 | 仅文件名级；逻辑级超范围需 LLM；plan scope 覆盖不全 |
| 回归风险 | ✅ 部分：`test_status` 字段 + 自证检测 | 中 | 只能风险排序；影响面分析需调用图 |
| 依据检测（从 discussion） | ❌ 证据源异构 | 中-高 | discussion 文本非结构化；「无依据 vs 没记录」不可区分 |

**commit 级自证定义**：该 commit 对应时间窗口内无 test 命令，且 commit message 或讨论声称测试通过（段级 `SelfClaimWithoutProof` 的 commit 级映射）。

### 6.2 依据检测的证据源分层（降难度设计）

1. **git 证据**（描述层，零 LLM）：commit message、diff 是否带测试、改动文件与问题域关系（问题域 = bug.files 字段 + root_cause 提及文件）
2. **bug/task 关联**（描述层）：commit 关联的 bug 记录、root_cause 具体性
3. **discussion 文本**（LLM 确认器，最后查）：前两层不足时才查，调用量最小化

**grounding 证伪的教训带入**：①「取证方向错」≠「无取证」；② 判定做分层（强依据/弱依据/证据缺失），「缺失」标注「可能是记录不全」，不当实锤。

### 6.3 实施（两阶段，不并轨防 scope creep）

- **第一阶段（描述层先行）**：`AnalyzeScopeDrift` + 自证检测 + diff 特征（改动规模/核心路径/测试缺失）→「可疑 commit 排序」。验收 = ED 历史回溯（已知问题 commit 排前）
- **第二阶段**：依据检测 = grounding 的产物侧映射，复用 L2 确认器基础设施

### 6.4 落地位置

- `aipmc review --since` 子命令（复用 eval 框架双输出：JSON + 人类可读），与 `aipmc eval` 并列
- **抽查反馈最小形式（P0 定义）**：`aipmc review --verdict <commit_id> ok|bad|unknown [note]`，反馈进 L3 校准集（8/14 反馈闭环决策的应用）
- 最终汇合：行为分析沙土节点 + review 可疑 commit 合并为**按风险排序的抽查聚焦清单**（L4 报告层，P0 阶段两侧分开可下钻）

---

## 7. 与现有组件的关系

| 组件 | 关系 |
|---|---|
| EVAL_PIPELINE S1 | L1 事件流依赖其 parse 归一化记录（已交付） |
| EVAL_PIPELINE S2/S3（turn/classify/segment） | **降级为工具级分析单位**（回合/工具序列），不再用于意图达成（§3） |
| MEASUREMENT_LOOP §4.3 意图抽查 | review agent 的种子——自动化扩展 |
| M 系列（HARNESS） | 测注入归因（harness 效果）；行为分析测过程质量——两套体系，报告层可合并 |
| eval/extract.go 自证检测 | review 回归风险的一部分（commit 级映射见 §6.1） |
| analyze/analyze.go AnalyzeScopeDrift | 抽到共享包，review 与简报共用（§6.1） |
| ED bug 库 | L3 校准集（root_cause 具体性评分）+ 问题实体锚点 + 验证样本 |

## 8. 风险与开放问题

| 风险 | 应对 |
|---|---|
| 跨项目比值不可比（01a00d3c 分母假象已实证） | 同 session 时间序列为主；agent 对照必须标注项目混杂；分母过小样本仅作参照 |
| 「无依据 vs 没记录」不可区分 | 分层判定 + 「证据缺失」标注，不当实锤 |
| LLM 判定漂移 | 带证据上下文 + 强制 JSON + 一致率 <80% 回修重跑（不调低标准） |
| **单维检测系统性误判（019ff89b 实证）** | 多维交叉：四子信号 + 形态可多标 + 声明轴独立；单维指标（检索密度/活动量）不得单独判健康 |
| 对象级数据覆盖不均（legacy vs hook） | 验收分层：命令级承诺 / 对象级方向性；覆盖限制表（§2.1）显式标注，缺对象级数据的样本不承诺「加深」 |
| **Goodhart 对抗（agent 适应检测点）** | 检测点不注入 agent 可见上下文；随机抽检 + 交叉验证（两套口径互证） |
| 8/19 相册渲染 session | **已定位**：`01a013f3-e6ca-7f20-8bde-de0414cabe4c`（8/18 16:19 - 8/19 11:30，427 行） |
| 工作区有 ~20 个未提交外来文件 | 只 add 本次改动文件，不 `git add -A` |

---

## 附：v1.0 → v1.1 修订记录（吸收 Claude 逐条审核 16 项）

| # | 类别 | 修订 |
|---|---|---|
| 1 | 事实错误 | §4「6/24 00:00-09:11（19h）」→ 修正为「6/23 13:51 起 19.3h，其中 6/24 00:00-09:11 的 9.2h 内自发检索=0」 |
| 2 | 事实错误 | §8 相册 session「待定位」→ 已定位 `01a013f3-e6ca` |
| 3-12 | 口径冻结 | 定向边界、≈0 阈值、纠偏关键词扩展、direction_ok 定义、root_cause 评分规则、误报判定规则、健康定义、0.41 分母标注、被动窗口统一 20min、补链归属规则 |
| 13-19 | 逻辑缺口 | P3 验收=行为指标对比（非达成）、回修重跑机制、纠偏可能误、S2/S3 降级、规格修订明确为 EVAL_PIPELINE 打标记、自证 commit 级映射、scope 抽共享包、verdict 反馈形式 |
| 20-21 | 缺失项 | 三要素画像（用户核心关切）补检测点定义 + L4 维度；Goodhart 对抗补风险条目 |

## 附：v1.1 → v1.2 修订记录（吸收 Claude 7 条 challenge）

| # | Challenge | 修订 |
|---|---|---|
| 1 | 区分度可能来自任务混杂 | 补做同 session 时间序列验证（§4.1 小时表）：死循环时段 vs 正常时段同一任务内分离清晰 |
| 2 | 候选只有检出没有准确性 | P0 第一步改为「候选→人工确认闭环」，输出误报率 |
| 3 | 规格数字与验证不同步 | §4.1 数字同步为冻结口径实测（0.05/1.72），记录口径变更来源 |
| 4 | fail=0 与候选机制矛盾 | 承认：初版候选只用 build+自发检索；P0 补 exit_code 失败检测或显式简化规格定义 |
| 5 | 验收②口径未定义 | 明确「盲猜样本 vs 健康对照 ≥5 倍」；8/3 只要求候选检出 |
| 6 | Xcode 构建环境噪声 | §2.1 补 build 噪声防护规则；01a013f3 候选时段核正为 0 |
| 7 | 候选与问题实体不对应 | 多问题 session（01a013f3）候选必须绑定问题实体（§3），P0 纳入 |

## 附：v1.2 → v1.3 修订记录（吸收 Claude 两轮 challenge 共 14 条）

| # | Challenge | 修订 |
|---|---|---|
| 1 | 分类学维度不统一（14:15 C1） | 两轴分类：过程行为轴 × 声明可信度轴；形态 4 归声明轴（复用 S3 自证检测） |
| 2 | 反馈响应性对 019ff89b 可能不低（14:15 C2） | 四子信号（响应/持续/加深/收敛）操作化；019ff89b = 响应✓ 持续✗ 加深✗ 收敛✗ |
| 3 | 休眠 vs 停滞（14:15 C3） | 活跃期口径 + N 标定（c0ad2534 跨夜 9h+ = 休眠负样本） |
| 4 | 加深度量（14:15 C4） | 转向后 N 分钟新对象比例（对象集合扩展率） |
| 5 | 重复调查判定（14:15 C5） | 对象重复计数 + 产出时限（N 分钟无 edit/commit → 标记） |
| 6 | 5/6/7 先定义再扫描（14:15 C6） | 形态 5-10 操作化定义先行，全库扫描放 P1 |
| 7 | 新核心与旧检测点映射（14:15 小点） | 旧「纠偏响应持续性」⊂ 新「持续」；旧「历史检索意识」= 「响应」的行为输入之一 |
| 8 | 加深在验收①样本不可算（14:28 C1） | 命令级/对象级分层；验收①只承诺命令级；对象级「加深」在 019ff89b 上另立方向性验收 |
| 9 | 形态语义（14:28 C2） | 形态 = 可多标特征标签，非互斥分类；019ff89b = 3+7+8 复合实证 |
| 10 | 形态 9 覆盖限制（14:28 C3） | exit_code 覆盖实测（legacy 8727/18542 vs post_tool 无结构化字段）；形态 9 标注 legacy 强信号 / hook 弱信号 |
| 11 | 伪进展 vs 正当排查（14:28 C4） | 判别式：加日志后是否有「根因定位 + 修复 commit」；8/14 019ffdce 8 处日志撤销为负例 |
| 12 | 健康基线（14:28 C5） | 基线矩阵（问题类型 × 四子信号）放 P1；P0 用同 session 前后时段对照 |
| 13 | 收敛不能用 agent 自报（14:28 C6） | 收敛 ground truth 外部化：用户确认 / 修复 commit（c0ad2534 = 11:37 确认 + d628b7a） |
| 14 | 规格膨胀（14:28 C7） | 阶段重切：P0 四子信号命令级 + 形态 1/2/3；P1 形态 5-10 扫描；形态 11-13 只进规格不进计划 |
