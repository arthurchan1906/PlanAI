# M 线产出物 3 — 7 天对比报告（模板，9/3 填充）

> 归属：task-20260827-111105-56a2fb（M 线，plan-80ed69）。
> 用途：9/3 数据窗口开启后，按本模板把实测数字填入即成报告。**本文件是模板/口径说明，不是报告本体。**
> 基线口径：`docs/METRICS_BASELINE-2026-08-27.md`（冻结 8/14-8/26）。权威计数源 = `[MCP]` 日志（P1a decision-20260827-131338-c95787）。
> 生成命令：`python3 metrics/mcp_compare.py --since 2026-08-29 --until 2026-09-03`（默认已排除 8/27-8/28 点破头 2 天）。

## 0. 口径与免责（务必随报告附上）

- **排除点破头 2 天**：8/27-8/28 是「查看审核/分析」指令驱动的密集调用日，**不可作为机制改善证据**；对比窗口从 8/29 起。
- **活跃日口径 + 缺口日标注**：基线 2297 仅覆盖 8 个活跃日，6 天（8/15/16/21/22/23）日志中断缺失。对比须同口径（活跃日日均），并标注窗口内缺口日。
- **低样本警示**：后窗口每个 agent 的活跃 session 数均为个位数（claude/codex <10）。**任何百分比在样本 <20 时都只作参考，不作改善证据。**
- **子代理/并行口径（关键）**：M0 漏录率在引用前必须排除标记为「子代理 / 并行短会话」的 session，否则会被误读为捕获泄漏。
  - 已知子代理短会话（8/29-9/1）：`614758e4`、`acaaf8ba`（各仅 1 次 `[LLM]`+`[INJECT]`，与已捕获父/伴生会话并行）。属预期口径边界，**非捕获泄漏**。
  - 实况：例中 claude M0 显示 `2/8 = 25.0%`，但其中 2 个即上述子代理 → **有效捕获泄漏率 ≈ 0%**（排除后为 `0/6 = 0.0%`）。
- **深度查证占比 ≠ 自发率**：该指标仅是「深度查证工具使用占比」的如实描述（基线 17.2%），**禁止**解读为代理「自发/主动」。D1 自发率已完成双标校准：严格双标 **76.0%**（未达 80% 门禁，below-gate）→ 24 条经第三方裁定收敛为 gold 共识集（`metrics/d1_gold_100.json`，codex/claude vs gold 均为 **88%**）；原始双标证据保留（未覆盖 baseline）。**下批复测 deadline：2026-09-24**（抽样窗口 9/10-9/21，排除 8/27+ 点破效应日）。

## 1. 数量指标（`[MCP]` 日志口径）

| 指标 | 冻结基线 8/14-8/26 | 后窗口 8/29-9/3 | Δ | 说明 |
|---|---|---|---|---|
| 总调用 | **2297**（8 活跃日，日均 **287.1**） | {fill} | {fill} | 仅 `aipm_*` 工具 |
| 日均（活跃日） | 287.1 | {fill} | {fill} | 同口径，标注缺口日 |
| 检索占比 | **64.5%** | {fill} | {fill} | 读/查类/全部 |
| 工具多样性 | **43 工具（H=4.278）** | {fill} | {fill} | 唯一工具数 |
| 深度查证工具占比 | **17.2%** | {fill} | {fill} | 代理信号，与 D1 自发率分开报 |
| 窗口内缺口日 | — | {fill} | — | 日志中断标注 |

MCP-ERR 分层（busy / no_rows / not_found / other）按 mcp_compare 输出填：busy 应保持 0（8/28 14:00 修复后清零）。

## 2. 质量指标（B 线检测器 51ff6d5 口径，30d 窗口）

| 指标 | 基线实测 | 后窗口 | Δ | 说明 |
|---|---|---|---|---|
| 实体引用 | 53 session / 545 refs | {fill} | {fill} | task/commit/bug/decision/plan/idea/thread |
| 漏查率（session 级，同层） | **11/61 = 18.0%** | {fill} | {fill} | 层级不同不可与引用级混比 |
| 漏查率（引用级，辅助） | 26/545 = 4.8% | {fill} | — | 仅供内部参考 |
| 查询→引用转化率（session 级） | 82.0% | {fill} | {fill} | 1 − session 级漏查率 |
| 数据源规范性（无来源词率） | 94.1% | {fill} | {fill} | 无区分度，不进反馈通道 |

## 3. M0 捕获完整性（per-session 漏录率）

> 命令：`aipmc metrics --baseline --since 2026-08-29T00:00:00 --skip_write`（由 `mcp_compare.py` 合并输出）。

| agent | [LLM] 总数 | 空 session | 漏录率 | 反向对账（脱链） | 结论 |
|---|---|---|---|---|---|
| claude | {fill} | **0** | {fill}（原始）/ **{fill}**（排除子代理后） | 0/6 | 排除 2 子代理后 ≈ 0% |
| codex | {fill} | **0** | **0.0%** | 0/6 | 无泄漏 |

- E 线闭环确认：`2026-08-28 12:37:58` 后**空 session = 0**（codex+claude 均 0）。
- 注册表依赖说明：M0 现按「当前项目 + `~/.aipmc/projects.json` 注册项目 + 全局日志绝对 `project=` 路径」聚合（bug-20260901-141137-acfabb 已修复），仅当某活跃项目既不注册又从未以绝对路径出现时才会误计。报告引用前确认两项目（aipmc + EncryptDrive）均已注册。

## 4. 验收锚点对照（METRICS_BASELINE §6）

1. **总调用**（活跃日均 287.1）变化 —— 同口径、标注缺口日。
2. **深度查证工具占比**（17.2%）是否提升 —— 代理信号，与 D1 自发率分开报。
3. **session 级漏查率**（18.0%）是否下降（B 线反馈回填 + briefing 消费后）。
4. **数据源规范性**（94.1%）是否随日志规范线改善。

## 5. 填写步骤（9/3）

1. `python3 metrics/mcp_compare.py --since 2026-08-29 --until 2026-09-03`（输出 JSON + 控制台摘要）。
2. 打开 `metrics/mcp_compare_2026-08-29_2026-09-03.json`，把 §1 数量指标、§2 质量指标（B 线检测器需另行跑）填入上表。
3. §3 M0 漏录率：把 claude 的 2 个子代理 session（`614758e4`/`acaaf8ba`）在正文标注为「子代理/并行」，并给排除前后两值。
4. 附口径免责（§0），低样本务必写明。
5. 提交 Claude 审核，再按审核意见修订。
