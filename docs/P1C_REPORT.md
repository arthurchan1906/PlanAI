# P1c 误报率报告 + 一致率验证（2026-08-26）

> 门禁物：按 EXECUTION_PLAN §P1 与 PROCESS_QUALITY_SPEC §2.3 产出——「20 例小标注集 + 候选→人工确认闭环（误报率报告）+ 判定一致率 ≥80% + 稳定性/对抗样本两层对抗（约束③）」。
> 数据源：`docs/P1C_ANNOTATION_SET.md`（19 例标注 + 2 对抗时段，人工判定列由 **Claude 8/26 逐条核查回填**，codex 只产候选与回填证据——观察者隔离，约束②）。
> 关联修复：捕获缺口 `bug-20260826-154305-941881`（52382a4 初始修复 + f67931a spool v2 + a6cfa49 DDL 守卫收敛）。

## 1. 样本构成（来源分层，约束①）

| 来源 | 说明 | L2 判定例数 |
|---|---|---|
| 开发 `01a00d3c` | AIPM 开发（proxy 路由调查），8/17-8/19 | 9 L2 + 1 形态6 |
| 产品 `01a013f3` | ED 分享 saga，8/18-8/20 | 8 L2 + 1 形态6 |
| 对抗（约束③） | c0ad2534 健康时段 ×2（runs=2） | 2 时段 4 判定 |
| **合计** | | **17 L2 判定 + 2 形态6 + 4 对抗判定** |

产品样本 8/17 ≈ 47%（规格要求 ≥5-8 例，达标）。校准口径建议：开发样本回归防退化、产品样本做验收（§2.3 约束①原文）。

## 2. 判定一致率（L2 vs Claude 人工判定）

| 检测点 | 例数 | 一致 | 不一致 | 一致率 |
|---|---|---|---|---|
| claim_classify | 6（D1/D3/D4/P1/P2/P3） | 6 | 0 | 100% |
| evidence_match | 1（D2） | 1 | 0 | 100% |
| feedback_response | 6（D7/D8/D9/P6/P7/P8） | 6 | 0 | 100% |
| deadloop_confirm | 4（D5/D6/P4/P5） | 1（D6，碰巧正确/证据不完整） | 3 | 25% |
| **合计** | **17** | **14** | **3** | **82.4%** |

- **严格口径（P5 计不一致）：14/17 = 82.4% ≥ 80% → 达标**（margin 2.4pp，薄）
- 宽松口径（P5「部分成立」计半一致）：15/17 = 88.2%
- 注：D7/P6 两条 L2 输出解析失败，经人工复核原始 LLM 输出判定，结论仍与人工一致

## 3. 误报率报告（候选→人工确认闭环）

| 检测点 | 候选数 | true | false（假阳性） | 部分成立 | 误报率（明确） |
|---|---|---|---|---|---|
| claim_classify | 6 | 6 | 0 | 0 | 0/6 |
| evidence_match | 1 | 1 | 0 | 0 | 0/1 |
| feedback_response | 6 | 6 | 0 | 0 | 0/6 |
| deadloop_confirm | 4 | 1（D6） | **2**（D5/P4） | **1**（P5） | 2/4（50%） |
| **合计** | **17** | **14** | **2** | **1** | **2/17（11.8%）** |

严格误报率（含部分成立）：3/17 = **17.6%**。

## 4. 误报根因（3 例全部同源 = 捕获缺口，已修复）

| 例 | L2 判定 | 人工判定 | 根因 |
|---|---|---|---|
| D5 | deadloop=true（0.95）「中间无 edit」 | **false** | JSONL 13:40:25 有 apply_patch 修复 metrics_test.go（日志缺失），真实序列=写测试→失败→修复→通过 |
| P4 | deadloop=true（0.85）「中间无修改」 | **false** | JSONL 17:36:11 有 apply_patch 修复 FilesDecryptTab.swift（日志缺失） |
| P5 | deadloop=true（0.9）「中间无修改/分析」 | **部分成立** | run1→run2 真盲试成立；run2→run3 之间 17:50:42 有 apply_patch 修复（日志缺失），「中间零修改」描述错误 |

**关键结论**：3 例不一致**全部**集中在 `deadloop_confirm`，且根因**全部**是 apply_patch 事件捕获缺失（`bug-941881`，丢失率 8/17 14%→8/20 78% 逐日恶化），而非 L2 判定逻辑本身错误。L2 在「窗口内无 write 信号」时判 deadloop=true 是**基于不完整证据的正确推理**——证据完整时（D6 之外无反例）判定可靠。

**修复状态**：捕获缺口三层修复已落地——① `store.LogDiscussion` 写失败落盘 spool（52382a4）；② spool v2 快速失败/时点透传/UNIQUE 冲突跳过（f67931a）；③ `openDiscussionDB` 去全量 DDL + flush 单连接（a6cfa49）。D5/P4 类窗口的 apply_patch 事件不再缺失，L2 输入证据完整性恢复。

## 5. 对抗样本（约束③，稳定性两层对抗）

| 时段 | 说明 | L2 判定 | 一致率 |
|---|---|---|---|
| c0ad2534 06-23 14h | 正常（讨论+检索，build=0 自发=1） | is_deadloop=false（0.97/0.95） | 100% ✓ |
| c0ad2534 06-24 11h | 修复验证期（纠偏→根因→修复） | is_deadloop=false（0.95/0.95） | 100% ✓ |

健康时段未被误判为死循环（runs=2 稳定，conf 漂移 ≤0.02）。样本有限（2 时段 4 判定），全量稳定性结论待独立观察期累积。

## 6. 已知局限

1. **形态 6 方向评估盲区**（D10/P9）：段内自发检索 ≥2 时 L2 不触发方向评估（§2.2），2 例均未触发、人工确认为非病态（105/36 转向均跨多任务响应指令）——这是**漏报面**而非误报，计入盲区不纳入一致率分母。
2. **样本量小**：17 例 L2 判定、82.4% 仅超线 2.4pp，且不一致全部集中在单一检测点；统计置信度有限，需独立观察期（约束④）累积产品样本复验。
3. **旧数据不可自愈**：spool 2026-08-26 引入，8/17-8/20 的丢失事件无 spool 记录无法补写；标注集判定维持现状（D5/P4 假阳性成立），修复效果以 8/26 后新 session 数据验证。
4. **证据完整性警告未全覆盖**：`eval process --l2` 已对「窗口内无 write 信号」的 deadloop=true 输出警告，但 D5/P4 类缺失恰在窗口**外**（候选窗口前后），JSONL 交叉校验仍是人工步骤。

## 7. 门禁结论

- ✅ **判定一致率 82.4%（严格）≥ 80%**——P1 验收达标（薄线通过，根因已修，观察期复验）
- ✅ **误报率报告已输出**：明确 2/17（11.8%），严格 3/17（17.6%），全部归因捕获缺口（已修复）
- ✅ **对抗样本通过**：健康时段 4/4 无误判
- 🔒 **阈值冻结观察期开始**（约束④）：P1c 误报率报告后，阈值冻结，**不立即用于 P3 注入**；期间以产品样本持续验证
- ⏭️ **下一步**：独立观察期累积 8/26 后新 session 数据（验证 spool v2 后捕获完整性 + 一致率复验）；P2 L4 画像聚合层可并行启动
