# 行为三维度基线(B11/B10) — 2026-09-08

> 归属:task-20260814-175921-4ff2f9(B10/B11) · 前置路2 论证(docs/BEHAVIOR_ROUTE2_ARGUMENT.md)
> 命令:`aipmc metrics --behavior [--since <date>] [--until <date>] [--json]` · 只读、一次性报告(非持续服务)
> 目的:给「行为侧路2 A/B」做前置的可复算信号 + **三维度行为基线(含同行感知, B12 扩为面板指标)**,替代需人工 D1 双标的自发率

---

## 数字(全量)

| 维度 | 数值 | 口径 |
|---|---|---|
| 解析覆盖率(B10) | **98.0%** | 能将 `role='tool'` 行解析成 `{ts,session,agent,tool,status}` 的比例(≥90% 达标) |
| 会话 / 调用 | 25 / 951 | `#aipm_*` MCP 调用(排除 🔧/👁 原生工具行) |
| **历史检索意识** | **70.5%**(670/951) | 检索类 aipm_* 调用占比(search/get/read/briefing/trace) |
| **计划性** | **48.0%**(12/25) | 用过 aipm plan/task 工具的会话占比 |
| **同行感知** | **44.0%**(11/25) | 用 `aipm_list_sessions` 查看跨 agent 状态板的会话占比(B12 新增) |
| **盲试检测** | **2.0%**(99/4916) | `[MCP]` 日志族 `status=ERR` 占比(跨全部归档,代理信号,见下) |

**后窗口 8/29-9/3** **检索 51.5%(136/264)、计划 50.0%(2/4)、覆盖 99.2%、会话 4/调用 264;盲试 2.6%(26/987)**。
**与 §0 M 线窗口对齐**;盲试按日志日从归档精确取数(旧版只读主日志且无直到上界,故此前报 2.9%/13/455 不可复算)。
同行感知(后窗口)50.0%(2/4)。

> **实时日志漂移注意**:全量 `[MCP]` 盲试分母随日志增长而变(4880→4916),因此**引用时必须固定窗口**(如 `--since 2026-08-29 --until 2026-09-03`)才可复算;「all」窗口是当前 snapshot 值。

## 方法与口径

- **B10 结构化事件流**:解析 `discussion_log role='tool'` 行的 tool 记号,兼容真实格式
  `🛠 mcp__aipm__aipm_<tool>`(claude-code)与 `📡 aipm_<tool>`;非 aipm 工具(🔧/👁/📝/🆕)排除。
  字段 `{ts, session_id, agent, tool, status}`。
- **B11 三维度 + B12 同行感知**:
  - 历史检索意识 = 检索类调用 / 全部 aipm 调用(检索类含 search/get/read/briefing/trace 等)。
  - 计划性 = 使用过 plan/task 工具的去重会话 / 总会话(按 session 去重,非调用计数)。
  - 同行感知 = 使用过 `aipm_list_sessions` 的去重会话 / 总会话(L1 同行感知,非调用计数)。
  - 盲试检测 = `[MCP]` 日志 `status=ERR` / aipm 调用(代理信号;discussion_log 不带工具状态)。
- **B12 面板**:以上指标已并入常规 `aipmc metrics` 面板的 `── [行为基线 B12] ──` 区
  (随 `--since` 窗口),方向性标注为「参考」,不作 ❌/✅ 门禁;`--behavior` 一次性报告保留。

## 口径边界(引用必读)

1. **盲试检测与其余两维不同源**:检索意识/计划性来自 `discussion_log`(有 session,可复算);
   盲试来自 `~/.aipmc/logs/aipmc.log*` 日志族(当前 + 归档)的 `[MCP]` 行(无 session)。
   主日志 `aipmc.log` 仅从 **9/2 14:07** 起(此前 8/29-9/1 在归档 `aipmc.log.20260902_140743`);
   `mcpErrRate` 按 `mcp_baseline.py` 的 glob `aipmc.log*` 模式跨归档读取,并按日志日 `[YYYY-MM-DD]` 精确对齐窗口。
   **勿将三维合并成单一行为分**。
2. **非因果**:本基线是方向性描述(同 9/2 路1 口径),不构成「行为 → 结果」证明;
   引用不得当作改善证据(模板 §0)。
3. **排除 auto**:用该基线做验收时必须只取人工 `review_status`,排除 `auto`(9/2 已实锤 auto 章假象)。
4. **未含持久化**:`--behavior` 不落库(只读),需留档请自行重跑或 `--json` 保存。

## 与路2 的关系

本基线是路2 A/B(同 agent skill 开/关)的**可复算前置信号**;先在无干预下记录此基线,
A/B 后再跑同一命令比对 delta,即可在「排除 auto、分列双目标」口径下检验强制净增量。
