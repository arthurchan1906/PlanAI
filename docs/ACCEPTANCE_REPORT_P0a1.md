# P0a1 验收报告（T1-T9，2026-08-24）

> 门禁物：按 EXECUTION_PLAN §9.2 提交对照物 + 验收①-③ 数据，供用户抽查。
> 数据源：ED pmai.db 副本（`/tmp/ed_t5_check.db`，2026-08-24 复制，c0ad2534 会话 788 条记录）。
> 复现：`aipmc eval process --session c0ad2534-a2da-4c35-8ef6-33207c5e9a91 --fix-hash d628b7a --db <副本>` / `aipmc eval acceptance ...`（JSON + 人类可读双输出，见文末命令）。
> 代码版本：commit `22b159b`/`238a7df`（含 Claude 六轮审核 20 challenge + 4 补充 + 自审 1 bug 全部闭环）。

---

## 1. 阶段完成状态

| 任务 | 内容 | 状态 | 对照物（§9.2） |
|---|---|---|---|
| T1 | 时段边界 | ✅ | start=2026-06-23 13:51:53（首条 user 消息）；end=2026-06-24 11:48:14（修复 commit d628b7a）；跨夜休眠 17:38:54→08:58:43 标记 |
| T2 | d628b7a 关联确认 | ✅ | fallback=partial（标题关键词命中 4 个：palv2/header/sizeof/ble）；关联 bug-20260624-114337-813aa9；weak=false |
| T3 | 反馈识别（用户介入） | ✅ | 介入=107 纠偏=25 推进=12 存疑=0 注入排除=0；25 次纠偏 ground truth 召回 100%（关键词命中恰好 25 条，8 条纯贴日志均不命中） |
| T4 | 历史检索三分类 | ✅ | 自发=2 / 被动=41 / 例行=11 / 比 0.049（mcp_tool 补盲后 36 条 aipm 检索入管道；2026-08-24 📡 text 行识别/双行去重后重算，见 §2 注记） |
| T5 | 死循环候选 | ✅ | 15h（build=16）/16h（build=11）候选命中 ✓；09h 未出候选（数据差异见 §4）；10h/11h 未误报 ✓ |
| T6 | 目标锚定（负样本验证） | ✅ | 15:09 vs 4b41ba8 = 对准（覆盖率 5/6=0.83，高频词资云集×3/打开×2 全命中，专名豁免），不误报为错位 |
| T7 | 构建产物完整性 | ✅ | 01a013f3 8/20 10:24 空壳构建检出 2 候选（CFBundleExecutable 占位符未替换 + ls total 空壳无主可执行） |
| T8 | 命令级五子信号 | ✅ | 107 反馈事件：响应 103 / 持续 71 / 收敛 104 / 对准 0（信息量标注：收敛=反馈早于 fix commit 的必然、对准 L1 保守全交 P1 L2） |
| T9 | 验收报告 | ✅ | 本文档 + `aipmc eval acceptance` 输出 |

---

## 2. 验收①-③ 数据结果表（真实数据）

| 验收 | 子项 | 结果 | 判定 |
|---|---|---|---|
| ① | 死循环召回 | 被标记正样本 **120/131 分钟 = 92%** ≥80%；09:00-09:11 的 11 分钟因 frozen 数据差异未验证 → **可复现召回 120/120 = 100%** | ✅ pass |
| ① | 负样本误报 | 被标记 **0 分钟**（≤15） | ✅ pass |
| ② | 同项目对照 | 01a013f3（自发/被动 = 48/41 = 1.17）vs c0ad2534（2/41 = 0.049）= **24 倍** ≥5 倍（48.6 为 mcp_tool 补盲前旧数、61/48/1.27/29 倍为 📡 text 行识别前旧数，均已不适用） | ✅ pass |
| ② | 自发/被动比方向性 | c0ad2534 自发≈0（0.049）vs 健康样本 1.17（01a013f3，实时计算 v1.4 定向边界口径；1.72 为 c5afe53d 跨项目 v1.2 旧口径仅参照），方向性成立 | ➡️ directional |
| ③ | 空壳构建单样本检出 | 01a013f3 10:25 空壳构建被规则命中（2 候选，证据=占位符+空壳 ls） | ✅ pass |
| ③ | 目标锚定负样本验证 | 15:09 vs 4b41ba8 判定对准（0.83），不误报为错位；有效性未验证（依赖 P1 正样本） | ✅ pass |

**验收①口径注记**：`BuildMin=10`/密集口径为冻结错误数字校准产物（§10.11），严格 isRealBuild 下无小时达标；判定方向（盲试/纠偏响应/修复期）依据外部事件边界表不依赖 build 数值——pass 不代表阈值可信，阈值合理性留 P1 全库扫描校准。

**验收① P0b 联动修订（2026-08-24，跨轮数据反馈 §9.6）**：P0b [01] 自评将 15h 桶（15:00-16:00）判定为 **uncertain**——该时段记录 = nm/objdump 反汇编定向深挖（方法性推进），「死循环/该用 aipm」均无实证（L2 待确认）。验收① 的 120 分钟正样本分账：**16h 60 分钟 = true（P0b 自评支持）；15h 60 分钟 = uncertain（定性待 L2）**。「可复现召回 120/120 = 100%」为 L1 候选命中口径，不承诺 15h 的死循环定性。本修订属跨轮变更（P0b 门禁后联动），不影响已过门禁的 P0a1 gating；L2 确认识别归 P1。

---

## 3. 关键对照物明细

### T1 时段边界 ↔ 事件边界表
| 冻结事件 | 时刻 | L1 输出 |
|---|---|---|
| session 首条 user 消息 | 6/23 13:51:53 | start=13:51:53 ✓ |
| 跨夜休眠 | 6/24 00:00-08:00 | 17:38:54→08:58:43（≥6h 且跨日）✓ |
| 用户纠偏后 8 次定向检索 | 6/24 09:11:46 | 纠偏识别命中（09:11:46 条，T3）✓；检索归被动窗口（T4 被动=41 含该批）✓ |
| 根因定位 | 6/24 11:04:12 | 事件表锚点 ✓ |
| 用户确认 | 6/24 11:37:15 | 事件表锚点 ✓ |
| 修复 commit | 6/24 11:48:14 | end=11:48:14（T2 锚定）✓ |
| 新 bug（KSN） | 6/24 11:50:08 | Cutoff=11:48:14 之后，不入本问题域 ✓ |

### T5 死循环候选 ↔ 冻结小时表
| 小时 | frozen 判定 | L1 结果 |
|---|---|---|
| 6/23 15h | 正样本（盲试）→ **P0b 自评 uncertain**（方法性推进：nm/objdump 反汇编深挖，见 §2 联动修订） | 候选命中 ✓（build=16 自发=0 被动=0） |
| 6/23 16h | 正样本（盲试） | 候选命中 ✓（build=11 自发=0 被动=0） |
| 6/24 09h | 正样本（09:00-09:11） | 未出候选（数据差异：frozen build=17 为散文指令误计，§10.11） |
| 6/24 10h | 负样本（纠偏响应） | 未误报 ✓（被动=18 排除） |
| 6/24 11h | 负样本（修复验证期） | 未误报 ✓（commit 锚点排除） |

### T6 目标锚定匹配表（15:09 vs 4b41ba8）
声称对象六词分法：第三方 / 打开方式 / 直传 / 文件 / 不跳转 / 不导入
共享 5/6（直传未命中）：第三方 ✓ 打开方式 ✓ 文件 ✓ 跳转 ✓ 导入 ✓
高频场景词：资云集×3（专名豁免）、打开×2（↔ 打开方式全命中）
覆盖率 = 5/6 = 0.83 ≥ 0.5 → **对准（无初始错位）**

### T7 空壳构建证据
- 10:24:03 / 10:24:23 两次 `xcodebuild ... build`（BUILD SUCCEEDED）
- 10:26:38 产物检查：CFBundleExecutable = `$(EXECUTABLE_NAME)` 占位符未替换
- 10:27:26 `ls .../EncryptDrive.app/`：total 16，无主可执行文件（空壳）
- 用户真机安装 Code 3000 → 空壳构建实证

---

## 4. 数据差异与归因（§9.6 数据反馈）

| 差异 | 处置 |
|---|---|
| 09h frozen build=17 不可复现 | 归因查明（§10.11）：frozen build 列 = 行级 content substring grep（09h 恰好 17 条「Build & Run」散文指令，零真构建）；验收① 11 分钟单独分账，不占可复现召回 |
| frozen build 数值系统性偏高 | 数值作废（§10.11）：16h/09h/10h 与现数据行级 grep 完全一致、其余差 ≤2（快照差异无 updated_at 无法溯源）；判定方向仍有效（外部事件边界） |
| T3 推进=12 语义噪音 | L1 关键词近似（「可以/好」含许可/疑问语义），已标注，P1 L2 精化 |
| T8 收敛/对准信息量低 | 已标注：收敛=反馈早于 fix commit 的必然、对准=0 无输出信息量（单 commit 样本），P1 多样本后才有判别力 |

---

## 5. 代码与测试验证

- **测试**：71 个测试全绿（T1-T9 正负例 + 2 个聚合层端到端）；`-race`、`go vet`、`gofmt` 全过。
- **覆盖率**：82.8%（pq_process 44.9%、pq_report 67.4%、pq_feedback 98.9%、pq_retrieval 100%）。
- **审核**：Claude 六轮审核（20 challenge + 4 补充）全部闭环；自审发现 T3 推进计数 bug 已修复。
- **归档**：`eval/archive/grounding.go`（build tag，不参与生产构建）。

---

## 6. 遗留待办（P0a1 边界内 + 门禁后）

| 项 | 归属 |
|---|---|
| 验收报告经用户抽查通过 → 进入 P0a2 | 门禁 |
| P0a2：主动触发 / 静态可核对 / P3 计数基线（重复验证点/自建记录利用） | P0a2 |
| P0b：019ff89b 复合标签方向性 + 10 候选时段人工确认（用户/Claude 三方抽检） | P0b |
| P1：形态 5-10 全库扫描 + L2 确认器 + 20 例小标注集 + 目标锚定正样本扫描（保留/删除）+ productProperNoun 启发式 | P1 |
| BuildMin=10 阈值合理性校准（冻结错误数字校准产物） | P1 |
| SleepRanges 消费（停滞时长判定接入 FindDeadloops） | P1 |
| 验收③「15:09 vs 4b41ba8」文案参数化 | P1 |

---

## 7. 复现命令

```bash
# 数据副本（ED 库只读，验收报告写输出文件不写库）
cp ~/projects/EncryptDrive/.pmai/data/pmai.db /tmp/ed_t5_check.db

# T1-T5 对照物（人类可读 + JSON）
aipmc eval process --session c0ad2534-a2da-4c35-8ef6-33207c5e9a91 --fix-hash d628b7a --db /tmp/ed_t5_check.db

# T9 验收①-③（人类可读 + JSON）
aipmc eval acceptance --session c0ad2534-a2da-4c35-8ef6-33207c5e9a91 --fix-hash d628b7a \
  --hollow-session 01a013f3-e6ca-7f20-8bde-de0414cabe4c --db /tmp/ed_t5_check.db \
  --anchor-msg "还有一个问题 从第三方应用选择图片或者文件 然后在打开方式中选择资云集 没有跳转资云集的界面 自己手动点击打开资云集才能看到有数据导入" \
  --claim "fix: 第三方「打开方式」直传文件不跳转/不导入 — SceneDelegate 转发 URL + 外部文件队列暂存"
```
