# 行为分析执行实施方案（EXECUTION_PLAN，配套 PROCESS_QUALITY_SPEC v1.4）

> 状态：**已冻结 + 执行就绪核查修订（修正版冻结，2026-08-20 用户确认）**——2026-08-20，codex 产出，从宏观到微观拆解规格落地；v1.1 吸收 Claude challenge 5 条 + 2 小点（hook 排除三层判定 / 关联 fallback / 反馈所指归属 / parse 扩展 / P0a1 再拆 / 口径澄清）；v1.2 吸收 Claude challenge 4 条（hook 排除改时代分层、ED 库 metadata 实证、反馈所指归属明确、回写一致性自查）；**v1.3 吸收 Claude 15:42 challenge 4 条 + ED/aipmc 双库实证（时代分层前提被推翻 → 改通道无关文本层判别、T8 对准近似验收定位），见文末修订记录**；**第 9 章（执行就绪核查修订草案）经 Claude 1708-1735 多轮 challenge + 用户确认，回写正文（§1 P0a1a/P0a1b、§2 T6、§3.1-3.3），进入 P0a1a 执行**。
> 前置：`docs/PROCESS_QUALITY_SPEC.md` v1.4（七轮修订 + §9 置顶锚与执行协议）。
> 执行纪律：按规格 §9——L0 锚七清单置顶、checkpoint 输出客观对照物、P0a1 完成提交验收报告（对照物 + 验收①-③ 数据）供用户抽查。

---

## 1. 宏观：执行阶段与里程碑

| 阶段 | 目标 | 产出 | 验收（规格 §5） | 前置 |
|---|---|---|---|---|
| **M0 冻结** | 规格 v1.4 用户冻结 | commit + AIPM 同步（review pending） | 用户明确「冻结/确认」 | — |
| **P0a1a**（1-1.5 天） | 地基 + 核心检测点（验收①②③ 必需的前 5 项） | T1-T5：时段边界 + d628b7a 关联 + 反馈识别（地基）+ 历史检索 + 死循环候选 | 验收①召回 ≥80% / 误报 ≤15min（依赖 T2 ground truth + T3 反馈识别地基）；T3 独立验收：25 次纠偏召回 ≥80% | M0 |
| **P0a1b**（0.5-1 天） | 验收③ + 五子信号 + 验收报告 | T6-T9：目标锚定（负样本验证）+ 构建产物完整性 + 命令级五子信号 + 验收报告 | 验收③ = 空壳构建单样本检出 + 目标锚定存在性验证（15:09 vs 4b41ba8 不误报）；T8 命令级五子信号输出 | P0a1a |
| **P0a2**（1 天） | 扩展检测点 | 信号-行为响应（命令级）/ 主动触发 / 静态可核对 / P3 计数基线（重复验证点/自建记录利用） | 方向性报告（§2.1 分层标注的 P0a2 层检测点均出候选） | P0a1 |
| **P0b**（1 天） | 019ff89b 复合标签方向性 + 候选→人工闭环 | 对象级加深方向性验证 + 10 候选时段人工确认 | 误报率报告 + 用户/Claude 三方抽检一致率 | P0a |
| **P1**（2-3 天） | 形态 5-10 全库扫描 + L2 确认器 | 形态 5-10 操作化扫描 + L2 五任务 prompt + 20 例小标注集 + P3 时间/存在性类基线定义 | 判定一致率 ≥80%；误报率报告 | P0b |
| **P2**（1-2 天） | L4 画像聚合 | 五子信号 + 形态标签 + 三要素 + 信号偏好矩阵 + 手段画像四分法 | agent 间/时间窗对照报告 | P1 |
| **P3** | aipm 改进闭环 | 9 项改进（§5 P3 表）绑定检测点前后对比 | 每项改进绑 L1 检测点前后对比（先测基线再改进） | P2 |

**阶段门禁**：每阶段完成 = 提交验收报告（对照物必查，§9.2）→ 用户抽查验收数据 → 通过后进入下一阶段。执行期规格修订以**数据反馈**为准（§9.6），不再讨论驱动。

---

## 2. 中观：P0a1 任务分解（T1-T9）

| # | 任务 | 实现内容 | 复用基础 | 输出对照物（§9.2） | 依赖 |
|---|---|---|---|---|---|
| T1 | 时段边界定义 | start = session 首条 user 消息时刻；end = 修复 commit d628b7a（11:48:14）；事件边界表（§4.1）为 ground truth；跨夜 6/24 00:00-08:00 标记休眠 | `eval/turn.go` Turn.Start / `eval/segment.go` Episode | 「时段边界 ↔ 事件边界表」映射表 | M0 |
| T2 | d628b7a 关联确认 + 25 次纠偏构成核实（v1.3 四轮） | commits 表核对 commit message 与 bug-20260624 标题一致 + 关联 bug 记录；验收① ground truth 钉死后再跑分离；**顺带核实 25 次纠偏 ground truth 构成（带文字 vs 纯贴日志）**——实证已先行完成：c0ad2534 关键词命中恰好 25 条、8 条纯贴日志均不命中（v1.3 四轮）。**关联不成立 fallback（v1.1 补）**：message 精确匹配 → 部分匹配（标题关键词 ≥2 命中）→ 时间窗口兜底（commit 在 11:48±1h 且 files 含 bug.files）→ 仍不成立则标注「弱 ground truth」，验收①相应降级为方向性检查，不硬凑 | sqlite 只读查询 `.pmai/data/pmai.db` | 关联核对证据（SQL + 结果行 + fallback 命中级） | T1 |
| T3 | 反馈识别（用户介入） | 纠偏候选识别：关键词（此前/之前/应该/查/历史/记录/提交记录/以前）+ 否定方向词（方向不对/不对/错了/重新审视）；**hook 注入行判别 = 通道无关文本层判别（v1.3 补，双库实证）**——实证：metadata 空/非空 = 写入通道分型（019f 前缀 hook 通道 6/26 起与空通道并存，非时代），用户手打与注入在两类通道均同形（01a013f3 注入 = role=user + metadata 非空 + `_type=user_prompt`；ca39669b 两者 metadata 均空）→ **metadata 不参与用户/注入判定**；判别只靠文本层——① 含 CJK 自然语言（含日志+描述混合）= 用户输入计入；② 纯结构化（`[模块]`+`=`+无 CJK，正则 `^(?:\[[A-Za-z0-9_-]+\][^\n]*\n?)+$`，**允许连字符**——`[ED-DEBUG]`/`[LV-DIAG]` 归此类，旧正则漏过致 3 条误归⑤）= **存疑候选交 L2，不硬判排除**（8/13 重复 8 条 `[VaultFlatten]` = 注入实证、「用户反复贴 5 次」为误读；但 L1 无法区分「用户纯贴日志无描述」vs「注入」——**漏报边界（v1.3 二轮）**：统一存疑交 L2 用上下文判定）；**P0a1 落点（v1.3 三轮）**：P0a1 无 L2——存疑候选只**标记存疑、不排除、不计入纠偏**，输出存疑清单与计数（方向性报告），P1 L2 判定后回填；③ 重复出现不做判别依据（v1.2 已删上下文层，实证维持）；④ 系统通知注入显式排除（`<task-notification>` XML 形态，**内容特征识别不依赖通道**；019f 通道 count=0 是数据分布事实）；⑤ 无 CJK 且非 `[模块]`（含连字符）行首且非系统通知 = **用户手动输入（证据/命令/确认）**，**计入用户介入（介入 ⊇ 纠偏两级计数，不参与纠偏验收计数）**（实证：c0ad2534 6 条、019f 通道 25 条——13 错误/回溯 + 8 命令/确认 + 3 `[ED-DEBUG]` 归②）。**来源判定（v1.3 六轮）**：metadata 与注入完全同形（`_type=user_prompt` 实证同）→ **不构成来源证据**；判定 = 内容形态学 + 时代（注入形态 = 应用日志 `[模块]` + `<task-notification>`，无落在错误/命令类的注入实证）；c0ad2534 时代可靠，019f 现代通道 = 高置信介入候选（P1 L2 确认）；**T3 实现分两类（v1.3 七轮，Claude 16:37 C1 边界确认）**——验收样本（c0ad2534 等无注入时代 session）⑤ 直接计入用户介入；现代通道（019f 等）⑤ 输出介入候选清单（P1 L2 确认），不直接计入；**25 次纠偏 ground truth 构成实证（v1.3 四轮）**：c0ad2534 关键词命中恰好 25 条、8 条纯贴日志均不命中——验收不受存疑规则影响；同时输出**反馈所指实体**（L1 正则候选：名词/文件/主题词；精确判定归 L2 `matched_object`，P1 阶段接入）；c0ad2534 已知纠偏 25 次 ground truth，召回 ≥80% | `eval/turn.go` Turn.UserMsg（metadata 仅通道标记，不参与判定） | 「识别结果 ↔ 25 次 ground truth」比对表（命中/漏报/误报） | T1 |
| T4 | 历史检索意识 | 定向检索计数 + 三分类（自发/被动/例行）+ 定向边界（git show<旧commit>/log/blame/grep/rev-list + aipm_search_*/get_*/list_commits/trace = 检索；git show HEAD:/status/diff/read_discussions = 不算）| `eval/extract.go` CmdSemantics（已有 git/query 分类，需扩展 aipm 工具） | 「检索事件 ↔ 定向边界规则」逐条分类表 | T1 |
| T5 | 重复停滞（死循环） | 候选标记：build 密集 + 失败 + 用户高峰 + 自发检索 <2 + **排除规则**（中间有 edit/commit/根因定位信号 → 不标）；build 噪声防护（排除 DerivedData/build 版本号/构建日志读取） | `eval/extract.go` CmdSemantics + ExitCodeSignal | 候选时段清单 + 正/负样本比对（验收①） | T3、T4 |
| T6 | 目标锚定（修正版冻结修订） | 场景词可提取的 session 首条 user 消息场景词 vs 首个 commit 声称对象；**对准判定 = 覆盖率 ≥0.5 + 高频词（≥2 次/单条消息）全命中**；**落地 = 负样本验证（存在性）**：01a013f3 15:09 vs 4b41ba8 期望「对准」（覆盖率 0.83），证明不误报；**待验证候选**：P1 无正样本则删除 | `eval/segment.go` Episode.IntentText + commits | 「场景词 ↔ 声称对象」匹配表（含阈值计算） | T2 |
| T7 | 构建产物完整性 | 构建类命令后校验 `*.app`/`*.appex` 含主可执行文件 + 关键 Info.plist 键进产物；单样本方向性（01a013f3 10:25 空壳构建被规则命中） | `eval/extract.go` CmdSemantics build 分类 | 「构建命令 → 产物检查」结果表 | T4 |
| T8 | 命令级五子信号 | 响应（纠偏后 20min 内新行为）/ 持续近似（旧对象不重现）/ 收敛（用户确认或修复 commit，不绑自报）/ 对准近似（响应对象 vs **反馈所指**——所指实体由 T3 提取并输出，T8 消费，不重复提取）；**对准近似 = 报告输出（方向性），不占验收①-③（v1.3 补——验收①-③ 无对准项，避免 P0a1b 卡在不可验收项）**；加深（对象扩展率）为对象级、P0a1 不承诺 | `eval/turn.go` 时间戳 + `eval/extract.go` 对象序列 + T3 输出 | 「五子信号 ↔ 子信号定义」判定表 + 证据 | T3、T4 |
| T9 | 验收报告 | 执行报告（含全部对照物）提交；比对验收①-③；输出 JSON + 人类可读 | eval 双输出模式（JSON + 人类可读） | 验收①-③ 数据结果表（对照物是用户抽查的抓手） | T1-T8 |

**P0a1 执行顺序**：T1 → T2（ground truth 钉死）→ T3（反馈识别 = 响应子信号地基，先于一切检测点）→ T4 → T5 → T6 → T7 → T8 → T9。

---

## 3. 微观：代码落点

### 3.1 复用基础（已交付，零改动或小扩展）

| 文件 | 复用点 | 改动 |
|---|---|---|
| `eval/parse.go` | `ParseToolRecord` → ToolRecord（事件流归一化） | **v1.1 补 + G1（执行就绪核查）**：ToolRecord 加 aipm 工具子类（`mcp__aipm__search_*`/`get_*`/`list_commits`/`trace` 细分——T4 定向边界依赖，当前 `normalizeToolName` `mcp__` 全归 mcp 大类不够细） |
| `eval/turn.go` | Turn（user 消息 + 记录序列 + 时间戳） | **G6（执行就绪核查）**：`isFakeUser` 仅排 `[Log]`/`[Progress]`，`[VaultFlatten]` 类注入成假回合——**P0a1a 用固定边界不重切分**（验收样本不受注入污染），BuildTurns 扩展留 P1 生产形态 |
| `eval/segment.go` | Episode（IntentText/Start/End/Commits） | 无 |
| `eval/extract.go` | `ExtractBehavior`（CmdSemantics 含 build/test/git/query、SelfClaimWithoutProof） | CmdSemantics 扩展 aipm 工具分类（T4 定向边界） |
| `analyze/analyze.go` | AnalyzeScopeDrift（P3 review 侧，P0a 不依赖） | 无 |

### 3.2 新增文件（P0a1）

| 文件 | 职责（对应任务） |
|---|---|
| `eval/pq_boundary.go` | T1 时段边界（start/end/休眠标记）+ T2 d628b7a 关联核对（数据查询函数） |
| `eval/pq_feedback.go` | T3 反馈识别（纠偏关键词 + hook 排除文本层判别 + 反馈所指提取 + 召回比对） |
| `eval/pq_retrieval.go` | T4 历史检索意识（三分类 + 定向边界 + 自发/被动窗口） |
| `eval/pq_deadloop.go` | T5 死循环候选（组合信号 + 排除规则 + build 噪声防护） |
| `eval/pq_anchoring.go` | T6 目标锚定（场景词提取 + 声称对象匹配 + 对准阈值 覆盖率≥0.5/高频词全命中 + 负样本验证输出） |
| `eval/pq_artifact.go` | T7 构建产物完整性（构建后产物校验规则） |
| `eval/pq_signal.go` | T8 命令级五子信号（响应/持续近似/收敛/对准近似） |
| `eval/pq_report.go` | T9 验收报告聚合（JSON + 人类可读 + 对照物输出） |

### 3.3 入口与数据

- **CLI**：`main.go` 注册 `aipmc eval process --session <id> [--since --until]`（复用 eval 双输出模式）
- **数据**：`.pmai/data/pmai.db` **只读**——`discussion_log` / `commits` / `bugs` / `session_summaries`（**G4：ED 库无 `sessions` 表**，T1 start 从 `discussion_log` 首条 user 消息直接计算）；`events` 表无分钟级事件边界（**G5**），§4.1 边界 = 规格冻结常量表（硬编码），非 events 查询；**验收报告/对照物写输出文件**（JSON + 人类可读，写 tmp 或工作区报告目录），不写数据库（v1.1 澄清「只读」约束仅限数据库）
- **测试**：`eval/pq_*_test.go`——用 §4 冻结口径数据（c0ad2534 死循环时段、25 次纠偏、01a013f3 目标错位/空壳构建）做单测断言

### 3.4 关键数据结构（示意）

```go
// 反馈识别（T3）
type FeedbackCandidate struct {
    UserMsgID string
    Ts        time.Time
    Kind      string // 纠偏 / 推进 / hook_注入(排除)
    Keywords  []string
}
// 死循环候选（T5）
type DeadloopCandidate struct {
    Start, End time.Time
    Builds     int
    SpontRetr  int
    Excluded   bool // 命中排除规则（edit/commit/根因定位）
}
// 五子信号（T8）
type FiveSignals struct {
    Responded bool
    Sustained bool
    Deepened  bool
    Converged bool
    Aligned   bool
    Evidence  []string
}
```

---

## 4. 执行协议绑定（§9）

| 实现单元 | checkpoint 对照物 |
|---|---|
| 每个新文件（pq_*.go） | 实现前：「L0 锚七清单 ↔ 本文件检测点」映射表；实现后：「规格条款 ↔ 实现」勾选表 |
| 每个检测点函数 | 实现中：验证证据（在 §4 冻结口径数据上运行，输出真实结果——不是「能编译」） |
| T9 验收报告 | 汇报时：验收①-③ 数据与规格口径核对表 |

**Goodhart 边界**（§9.4）：L0 锚注入行为准则；L1 检测点阈值/规则不注入 agent 可见上下文。

---

## 5. 风险与应对

| 风险 | 应对 |
|---|---|
| 规格未冻结就开工 | P0a1 以用户冻结确认为门禁（M0）；当前为草案仅供评审 |
| legacy/hook 数据覆盖不均 | 覆盖限制表（§2.1）：命令级承诺 / 对象级方向性；T8 加深不承诺 |
| 反馈识别召回不达标（<80%） | T3 独立验收点：召回 <80% 先回修识别规则再跑其余检测点（地基） |
| 执行退化（简化/拍脑袋） | §9 锚定执行：checkpoint 对照物必查 + P0a1 验收报告供用户抽查 |
| diff 与外来文件混淆 | 只 add 本次改动文件（`docs/EXECUTION_PLAN.md` + P0a1 代码文件），禁 `git add -A` |

## 6. v1.1 修订记录（2026-08-20，Claude challenge 5 条 + 2 小点）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | T3 hook 排除正则误伤用户手动贴日志（用户贴证据 = 用户介入信号）；且 metadata 来源标记（`hook_event_name=UserPromptSubmit` 存在于所有 user 消息）不足以直接区分 | **成立（实质）** | T3 排除改**三层判定**（来源层 tool 记录 / 文本层 CJK / 上下文层重复出现）；首次纯结构化存疑交 L2；实证：aipmc 库 user 消息全部带 UserPromptSubmit 标记，来源判定不可单独用 |
| 2 | T2 未处理「关联不存在」（bug→commit 关联率仅 15%） | **成立** | T2 补 fallback 链：精确 → 部分匹配 → 时间窗口兜底 → 弱 ground truth 降级 |
| 3 | T8「反馈所指」提取无任务归属 | **成立** | 所指实体提取归 **T3**（同一消息解析），T8 消费 |
| 4 | T4 依赖 aipm 工具子类，但 parse.go 标「无」改动 | **成立** | §3.1 标注 parse.go 扩展：ToolRecord 加 aipm 子类（search/get/list_commits/trace 细分） |
| 5 | P0a1 九个任务 + 8 映射表在 1-2 天内偏紧 | **成立** | P0a1 拆 **P0a1a（T1-T5 地基）/ P0a1b（T6-T9）**；T3 独立验收点单列 |
| 6 | 「数据只读」与验收报告写文件的约束冲突 | **成立** | 澄清：数据库只读，验收报告写输出文件 |
| 7 | T4 定向边界 aipm 工具口径（get_task=list_commits 当前态 vs 历史）需冻结 | **成立** | 口径冻结列入 P0a1（回写规格 §2.1）：`search_*`/`trace` = 历史检索；`get_*`（task/plan/commit 详情）+ `list_commits` = 状态读取（当前态），**不计入历史检索意识** |

**回写规格**：① §2.1 用户介入 hook 排除补三层判定；② §2.1 定向边界 aipm 工具子类口径（与上表第 7 条一致）。

## 7. v1.2 修订记录（2026-08-20，Claude challenge 4 条 → ED 库数据实证）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | 上下文层「重复 ≥2 次 = hook 注入」误伤用户重复贴日志（ED saga 8/13 用户反复贴 5 次 = 关键介入信号）**〔注：此断言经 v1.3 实证推翻——8/13 重复 8 条为注入非用户贴，见 §8 记录 5〕** | **成立（实质）** | **删除上下文层**；hook 排除只依赖来源层；T3 改按 session 时代分层（旧 session 不排除） |
| 2 | ED 库旧格式 user 消息可能无 hook 标记，来源层在验收样本不可用 | **成立（实质），实证修正** | ED 库实证：c0ad2534 107 条 user 消息 **metadata 全空**、01a013f3 105 条 **全非空**；且 `[VaultFlatten]` hook 注入 = role=user + metadata 空。推论：旧 session（c0ad2534 时代）**本无 hook 注入**，不启用排除；现代 session 按 metadata 来源层判定（非空=用户输入，空+纯结构化=hook 注入） |
| 3 | 「反馈所指实体」提取逻辑无任务归属（L1 正则 vs L2 语义） | **成立** | T3 内实现 **L1 正则候选**（名词/文件/主题词），精确判定归 L2 `matched_object`（P1 接入）——与规格 §2.1 对准子信号口径一致 |
| 4 | 规格 §2.1 回写需抽查与执行方案一致 | **成立** | 自查并同步：规格 §2.1 用户介入行同步改时代分层（删上下文层）；定向边界 aipm 子类口径两文件一致 |

**实证数据（v1.2）**：ED 库（cp 到 /tmp 只读查询）：c0ad2534 user 消息 107/107 metadata 空；01a013f3 user 消息 105/105 metadata 非空；`[VaultFlatten]` 记录 = role=user + metadata 空。

## 8. v1.3 修订记录（2026-08-20，Claude 15:42 challenge 4 条 → ED/aipmc 双库实证）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | 时代切点 8/10 未落盘——7 月中旬-8/10 之间 session（metadata 可能非空但无 hook 注入）会被误归「现代」启用来源层判定 | **成立（实质），实证升级** | 实证推翻「时代分层」前提：metadata 空/非空 = **写入通道分型**（019f 前缀 hook 通道 6/26 起存在，与空通道并存至 8/20；aipmc 库同样混合），不是时代。T3 改**通道无关文本层判别**（不再按 session 时代/日期分型） |
| 2 | 来源层判定需按库分型——ED 库（空=注入）与 aipmc 库（全部 UserPromptSubmit）逻辑相反，同套代码跨库会错 | **成立（实质），结论更彻底** | 双库实证：用户手打与注入在**两类通道均同形**（01a013f3 注入 = role=user + metadata 非空 + `_type=user_prompt`，与用户输入完全同形；ca39669b 两者 metadata 均空）→ **metadata 来源层在任何库/通道均无判别力，统一文本层判别**，不按库分型 |
| 3 | 切点应按注入形态存在性而非日期一刀切 | **成立** | T3 判别不依赖日期阈值；「session 是否含纯结构化注入记录」仅作 L2 辅助背景，不参与 L1 排除 |
| 4 | T8 对准近似的验收基准未定——验收①-③ 无对准项，P0a1b 会卡在不可验收项 | **成立** | T8 明确：**对准近似 = 报告输出（方向性），不占验收①-③**；验收①-③ 维持不变（死循环切分/方向性对照/目标锚定+空壳构建单样本） |
| 5 | （自查）v1.2 记录 1 的实证前提「用户 8/13 反复贴同一日志 5 次 = 关键介入信号」经双库实证为**对注入的误读** | **成立（实证纠错）** | 8/13 重复 8 条 `[VaultFlatten] flatten done: flattened=0`（11:37/13:03/13:18/13:27/14:10/17:00/17:43/17:49，loadAssets count 递增）= **hook 注入非用户贴**；用户贴日志实证（8/13 10:31/13:05 lldb/16:02、8/19 17:31/17:43）全部含自然语言描述。规格 §2.1 与 T3 已按此修正表述 |

**实证数据（v1.3，ED 库 cp /tmp 只读 + aipmc 库只读）**：① ED 库按天统计 user 消息 metadata 空/非空**同日混杂**（6/15 非空 32/空 25；7/24 空 86/非空 4）——非时代信号；② ED 库 session 级：019f 前缀通道 6/26 起全非空（如 019f02e7 = 6/26），非 019f 通道至 8/20 仍全空（47502f63、c21b4c20）——通道分型；③ 01a013f3：user 95 条自然语言（metadata 非空）+ 14 条 `[VaultFlatten]` 结构化（metadata 也非空、`_type=user_prompt` 同形）；④ ca39669b（8/10，metadata 空通道）：用户手打（「你为什么不一次到位获取足够多的全链路日志」）与 `[VaultFlatten]` 注入 metadata 均空；⑤ aipmc 库：user 消息 1269 空 + 1234 非空（session 级分型），当前 Claude session 8a866a42 = 146 条全空。

## 8.1 v1.3 二轮修订记录（2026-08-20，Claude 15:53 challenge 2 条 + 复盘来源实证核实）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | Claude 承认「用户反复贴 5 次」是它基于 **ED agent 13:59 复盘自述**的推断、未实证（误读），连带要求**修正 ED agent 的复盘**（跨会话知识修正） | **误读承认成立；连带修正无对象（实证）** | 核实：**「ED agent 13:59 复盘自述」在 ED 库（discussion_log/session_summaries/events/daily_notes）、aipmc 库、ED 本地 Claude JSONL（28d89e1a 有、019ff89b 无本地文件）中均无原文**——待办无对象可执行；Claude 的误读链条（ED 复盘 → Claude 引用）缺实证一环，记录为「观察者未实证」又一实例。8/13 重复 8 条 `[VaultFlatten]` = 注入的判断维持（v1.3 已落盘） |
| 2 | 文本层判别漏报边界：用户**纯贴日志（无自然语言描述）**会被判为注入而漏掉用户介入信号；实证仅 5 样本 | **成立** | §2.1/T3：纯结构化 user 消息由「排除」改「**存疑候选交 L2**」（用上下文判定，宁留噪音不漏用户信号）——恢复 v1.1「存疑交 L2」机制但不恢复「重复 ≥2 次 = 注入」上下文层；标注 5 样本局限（8/13 三次 + 8/19 两次均带自然语言，纯贴日志场景未被覆盖） |
| 3 | （肯定项）codex 主动双库实证 + 主动推翻自己 v1.2 落盘 = 设计者校准原则正样本 | 采纳 | 记录为**设计者校准原则正样本**（区别于此前 4 个负样本实例），入规格附录 v1.3 二轮记录 |

## 8.2 v1.3 三轮修订记录（2026-08-20，Claude 16:06 challenge 2 条：错误事实引用检查 + P0a1 存疑落点）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | Claude 核实后承认误读链条完整还原（数字错 5≠4、解读错、**来源错——它把查询数据的推断写成了事实**），要求 codex 检查规格/执行方案是否引用「用户反复贴 5 次」错误事实（担忧污染 §4.6 样本） | **检查通过** | 活动文本（§2.1/T3）均已标注「为误读」，未被当事实引用；本表 v1.2 记录 1 已加注「实证推翻」；口径补充：8/13 `flattened=0` 注入按窗口计数不同（13:00-14:30 窗口 4 条 vs 全天 8 条），「4 条」与「8 条」均为注入、窗口不同不矛盾，「5 次」是 Claude 追踪计数错误 |
| 2 | **P0a1 无 L2（L2 归 P1）**——「纯结构化 → 存疑交 L2」在 P0a1 落点未定义，存疑候选交给谁？ | **成立（实质）** | §2.1/T3 补 **P0a1 存疑落点**：存疑候选在 P0a1 只**标记存疑、不排除、不计入纠偏**，输出存疑清单与计数（方向性报告），P1 L2 判定后回填；不影响 T3 纠偏召回统计（纯结构化不参与纠偏关键词匹配，用户贴日志带自然语言已被①覆盖） |
| 3 | （记录项）本轮 = 设计者校准最完整实例（codex 实证质疑 → Claude 核实 → 承认更深错误 → 修正），完整「假设→验证→修正」闭环发生在设计者之间 | 记录 | 入规格附录 v1.3 三轮记录 |

## 8.3 v1.3 四轮修订记录（2026-08-20，Claude 16:14 challenge 2 条：纯贴日志与纠偏验收 + 记录中立性）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | **实质待确认**：纯结构化 → 存疑 → 不排除也不计入纠偏，用户纯贴日志（无自然语言）在 P0a1「消失」；需核实 25 次纠偏 ground truth（c0ad2534）是否含纯贴日志场景（含 → T3 召回受威胁；不含 → 验收不受影响），建议 T2 顺带核实构成 | **验收不受影响（实证）；暴露规则缝隙（补充规则）** | 实证：c0ad2534 按 T3 纠偏关键词命中**恰好 25 条** = ground truth；8 条无 CJK 消息（纯贴编译错误/崩溃/诊断日志）**全部不命中**纠偏关键词 → 25 次 ground truth 不含纯贴日志场景，T3 纠偏召回 ≥80% 验收不受存疑规则影响。**但实证发现规则缝隙**：无 CJK 且非 `[模块]` 的 user 消息真实存在（c0ad2534 8 条、019f 通道 25 条如 `Failed to install...`/`compile fail`/`No symbol named...`），另有 `<task-notification>` 系统注入（76 条仅非 019f 通道）——T3 补 ④ 系统通知显式排除 + ⑤ 无 CJK 非模块非通知 = 用户贴证据计入用户介入；T2 已并入构成核实 |
| 2 | 记录中立性：「最完整实例」应包含双方错误（codex v1.2 时代分层也是 codex 自己写的、被自己的新实证推翻），只强调 Claude 错误是记录偏差（功劳叙事在「认账」方向的另一种形态） | **成立** | 规格 v1.3 三轮记录改**「双方各犯一错、互相实证修正」**中立表述（codex：假设性错误——设计假设被自己实证推翻；Claude：事实性错误——数据解读把推断写成事实；不评「更深」） |
| 3 | （T2 落地项）25 次 ground truth 构成核实归 T2 | 采纳 | T2 任务表补「顺带核实 25 次纠偏构成（带文字 vs 纯贴）」——实证已先行完成：25 次全部含 CJK、无纯贴日志 |

## 8.4 v1.3 五轮修订记录（2026-08-20，Claude 16:21 challenge 4 条：规则⑤来源验证 + 规则④通道澄清 + 介入/纠偏语义 + 中立性去「更深」）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | **实质**：规则⑤「无 CJK 非 [模块] = 用户贴证据」判定过宽，25+8 条真实来源未验证（用户手打/hook 捕获/其他注入？）；与「metadata 无判别力」对照，来源凭什么判定 | **来源已验证（逐条实证）** | 25+8 条 metadata 全 `_type=user_prompt`（用户提交通道）+ 上下文佐证（编译错误前有用户提问、后接用户纠偏如「你在干什么 为什么一个功能修改了很长的时间」）。25 条构成：13 错误/回溯（`Failed to install`/`compile fail`/`No symbol named`/`(lldb) bt`/`The application could not be launched`）+ 8 命令/确认（`&aipmc-model switch/current`×5、`go on`/`ok`/`push`）+ **3 条 `[ED-DEBUG]`/`[LV-DIAG]` 结构化日志因模块名带连字符被旧正则漏过（误归⑤）**。修订：**判别正则扩展允许连字符**（`[A-Za-z0-9_-]`），`[ED-DEBUG]` 等归②存疑；⑤ 仅覆盖错误/回溯/命令/确认 |
| 2 | 规则④「仅非 019f 通道」与「统一文本层」结论矛盾——识别靠内容特征还是通道？ | **澄清** | ④识别靠**内容特征**（`<task-notification>` XML 前缀），不依赖通道；019f 通道 count=0 是数据分布事实（实证），非判定条件，与「统一文本层」一致 |
| 3 | 规则⑤「计入用户介入」与「纠偏」语义未区分（介入是纠偏超集）——若混用 T3 纠偏会膨胀 | **成立** | 明确**介入 ⊇ 纠偏两级计数**：⑤计入用户介入（供用户介入检测点密度），不参与纠偏计数（供反馈识别验收）；25 次纠偏 ground truth 不受影响（均含 CJK） |
| 4 | 记录中立性「更深」是主观定性 | **成立** | 中立性改客观描述：codex=假设性错误（设计假设被自己实证推翻）、Claude=事实性错误（数据解读把推断写成事实），不评「更深」 |

**实证数据（v1.3 五轮，ED 库 cp /tmp 只读）**：019f 通道「无 CJK 非 `[模块]`」user 消息 25 条全列——13 条错误/回溯 + 8 条命令/确认 + 3 条 `[ED-DEBUG]`（带连字符模块名）+ 1 条 `(lldb)` 归错误类；metadata 全 `_type=user_prompt`；c0ad2534 8 条 = 2 `[PalService]`（归②存疑）+ 6 编译错误/崩溃（归⑤）；019f 通道 `<task-notification>` count=0（非 019f 通道 76 条）。

## 8.5 v1.3 六轮修订记录（2026-08-20，Claude 16:30 challenge 3 条：`_type=user_prompt` 判别力矛盾 + ⑤语义 + ④通道解释）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | **实质（证据链矛盾）**：codex 用 `_type=user_prompt` 证明「用户输入」，但它自己 15:50 实证过 [VaultFlatten] 注入也是 `_type=user_prompt`——同一字段两个结论；若两者同形，「来源已验证」不成立，规则⑤来源判定悬空 | **成立（认账）** | 精确对比实证：注入与用户手打 metadata **完全同形**（`_type=user_prompt`/`hook_event_name=UserPromptSubmit`/`prompt==content`）——**metadata 不构成来源证据**，撤回「来源已验证」表述；来源判定改**内容形态学 + 时代**（注入形态 = 应用日志 `[模块]` + `<task-notification>`，无落在「错误/回溯/命令/确认」类的注入实证；c0ad2534 验收样本 6 月无注入时代来源可靠；019f 现代通道 25 条 = 高置信介入候选，P1 L2 确认）。T3 纠偏验收不受影响（25 次 ground truth 含 CJK，不依赖规则⑤） |
| 2 | `&aipmc-model switch` 是用户 CLI 命令不是「贴证据」——⑤语义混类 | **成立** | ⑤语义改「**用户手动输入（证据/命令/确认）**」，与「贴证据」区分（影响手段画像的交互手段分类） |
| 3 | 规则④「019f count=0」未解释——不阻塞 | 记录 | 019f = Claude Code hook 通道（`UserPromptSubmit`）；`<task-notification>` 为其他工具通道注入形态——通道差异记录为数据分布事实，内容特征识别不变 |

**实证数据（v1.3 六轮）**：`01a013f3` 注入 `[VaultFlatten]` 与用户手打 metadata keys 相同（`_type/cwd/hook_event_name/model/permission_mode/prompt/session_id/turn_id`）、`hook_event_name=UserPromptSubmit`、`prompt==content`——完全同形；注入内容形态 = 应用日志（`[VaultFlatten]`/`[LocalVaultStore]`/`[DIAG]`/`[ED-DEBUG]`/`[PalService]`）+ `<task-notification>`，25 条中 13 条错误/回溯 + 8 条命令/确认的内容形态（编译错误/lldb/CLI 命令）与注入不同，无落入该类的注入实证。

## 8.6 v1.3 七轮修订记录（2026-08-20，Claude 16:37 边界确认 2 条 + §9.6 收敛判定）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | 边界确认：c0ad2534 8 条（时间事实保证无注入）直接计入用户介入 vs 019f 25 条高置信候选交 P1 L2——T3 需明确实现区分（不统一按规则⑤） | **已落实** | T3 补「实现分两类」：验收样本（无注入时代 session）⑤ 直接计入；现代通道（019f 等）⑤ 输出介入候选清单（P1 L2 确认） |
| 2 | 记录：「注入无错误/回溯/命令/确认类形态」是现有样本归纳——未来注入形态（如 hook 捕获编译输出）可能落入⑤ | 记录（不阻塞） | §2.1 补**归纳边界**：P1 L2 判定时留意未来注入形态落入⑤的可能 |
| 3 | **收敛判定（§9.6）**：本轮 3 条已修、无新实质设计问题 → 实质问题数=0 → 收敛；关键事实链闭合（T3 验收不受规则⑤影响 / c0ad2534 来源可靠 / 25 条候选交 P1 / metadata 同形实证 / 时代假设推翻修正 / 「5 次」误读修正） | 接受收敛宣告 | 按 §9.6 防表演原则不再制造新挑战；实质判定以用户冻结评审为准 |

**关键事实链（收敛时闭合）**：① T3 纠偏验收（25 次 ground truth 含 CJK）不受规则⑤影响；② c0ad2534 8 条来源（时间事实）可靠；③ 019f 25 条 = 介入候选交 P1 L2；④ metadata 同形（来源层无判别力）实证确认；⑤ 时代假设（v1.2）被推翻且修正；⑥ 「用户反复贴 5 次」误读修正（8 条 = 注入）。

**待用户冻结评审**：确认后 commit（只 add `docs/PROCESS_QUALITY_SPEC.md` + `docs/EXECUTION_PLAN.md`）→ 进入 P0a1a 执行（T1-T5 地基 + 锚定执行 + 验收①③）。

---

## 9. P0a1a 执行就绪核查修订草案（v1.4，2026-08-20，codex 执行前核查产出）

> 状态：**已回写正文（修正版冻结，2026-08-20 用户确认）**——冻结后 codex 对执行层面做实证核查（代码落点 + ED 库数据可得性），发现 8 项缺口；经 Claude 多轮 challenge（1708/1713/1717/1721/1733/1735）收敛，实质问题清零；用户确认后**回写正文（§1 P0a1a/P0a1b、§2 T6、§3.1-3.3 及 SPEC §1/§2.1/§4.6/§5/§9.1）**。判定均为 codex 建议，最终以用户冻结评审为准。

### 9.1 P0a1a 第 0 步：执行就绪核查（正式化）

**「讨论收敛 ≠ 执行就绪」（Claude 1708 C2）**：§9.6 收敛判据只覆盖设计层。冻结后立即发现 8 项缺口证明需补执行层核查。**P0a1a 第 0 步 = 执行就绪核查**，checklist 模板 = 本节 9.2 的 G1-G8（代码落点/数据结构/数据源/依赖四类）。

### 9.2 执行缺口清单（G1-G8，实证核查）

| # | 缺口 | 实证 | 修订动作 |
|---|---|---|---|
| G1 | `normalizeToolName`（`eval/parse.go:305`）`mcp__` 全归 mcp 大类 | 代码核查：`strings.Contains(lower,"mcp")` 先判，aipm 子类无法区分 | ToolRecord 加 aipm 工具子类（`search_*`/`get_*`/`list_commits`/`trace` 细分）——T4 定向边界依赖（§3.1 已标注未落地） |
| G2 | `classifyCommand`（`eval/extract.go:132`）只处理 bash 命令 | 代码核查：分类表 vet/test/build/git/query/deploy/other，aipm 工具未接入 | CmdSemantics 扩展 aipm 工具分类——T4 三分类（自发/被动/例行）依赖 |
| G3 | `aipmc eval process` CLI 不存在 | `main.go` eval 仅支持 `attribution` kind | 新增 `aipmc eval process --session <id>` 入口（复用 eval 双输出模式） |
| G4 | ED 库**无 `sessions` 表** | `.tables` 实测：仅 `session_summaries` | §3.3 数据源列表修正；T1 start 从 `discussion_log` 首条 user 消息直接计算 |
| G5 | `events` 表**无分钟级事件边界** | 6/23-25 仅 2 条 `task_created` | §4.1 边界 = **规格冻结常量表**（硬编码），非 events 查询；`pq_boundary.go` 明确此点 |
| G6 | `isFakeUser`（`eval/turn.go:102`）过窄 | 仅排除 `[Log]`/`[Progress]` 前缀；`[VaultFlatten]` 类注入成假回合 | T3 文本层判别（①-⑤）接入点待定：扩展 BuildTurns（影响现役 attribution 管道）vs pq_feedback 重切分——**P0a1a 决策预判（1735 补）**：验收样本均不受注入污染（c0ad2534 早于 019f 通道 6/26、01a013f3 用 15:09 真实用户消息）→ **P0a1a 用固定边界不重切分**，BuildTurns 扩展留 P1 生产形态；第 0 步按此预判执行，仅当验收样本被注入污染时才重议 |
| G7 | T6「session 首条 user 消息」对 `01a013f3` 不成立 | 会话跨 3 天（8/18-8/20）多子任务，首条消息是另一任务 | 改「子任务事件边界切分后首条」；Episode 可复用（`forced_by_task` 边界）但无 LLM 降级会过度切分（见 9.3） |
| G8 | 规格「两张图片」场景词引用错误 | 该短语仅出现在 8/19 16:44 注入日志（`[VaultFlatten]`），非用户原话 | hindsight 修正（见 9.4） |

### 9.3 目标锚定/T6 修正（Claude 1717/1721 收敛）

**实证事实链**：
- `01a013f3`：8/19 15:09 用户原话「选择图片**或者**文件…打开方式…没有跳转」，`4b41ba8`（15:33）标题「第三方『打开方式』直传文件不跳转/不导入」——**精确对准，无初始错位**；8/20 10:35 才首次出现「多选」需求（8/19 全天用户消息无多选表述）
- `019ff89b`：8/13 09:10 首条 user 消息「设备下拉列表状态点…对比安卓实现」，首 commit 09:53「fix(iOS): 设备下拉列表状态点…」——**精确对准**；16:33「自以为是」爆发针对 16:02 报告的对齐失败，非初始错位
- 归纳边界（Claude 1721 C1 采纳）：**bug 报告类样本初始错位罕见**（表面诉求锚定）；**模糊指令类输入前提不满足**（无场景词 → 检测点不可判定，而非可检出错位）。「伪检测点」改标**「样本不足」**，P1 全库扫描（形态 5-10）作为样本可得性最终确认

**修订决策（四条，Claude 1721 建议 + codex 修正）**：
1. **目标锚定**：保留定义，降级为**定义层**（无 P0a 验收承诺），标注「样本不足：bug 报告类罕见、模糊指令类输入前提不满足」
2. **需求对齐**（10:35/10:44 点破 → 10:52 agent 校准）：并入 **T8 对准**，标注「**跨子任务粒度**」边界（区别于 T8 原有子任务内粒度）；跨子任务粒度依赖 Episode 子任务切分增强（P1）
3. **T6 落地形态** = **方向性报告（负样本验证）**：在已知对准的 15:09 vs 4b41ba8 上运行，期望输出「对准」，证明检测点**不误报**（非空洞「能编译」）
4. **主体修正**：规格「10:44 才校准」→「**10:44 用户点破，10:52 agent 校准**」（点破=用户动作，校准=agent 动作）

**T6 负样本验证的对准阈值（Claude 1728 C1 补，防「期望输出对准」变成主观断言）**：
- 场景词提取：用户原话名词/主题词去停用词（实证：15:09 场景词 = 图片/文件/打开方式/资云集/跳转/数据导入）
- 声称对象提取：首个 commit 标题关键词（实证：4b41ba8 = 打开方式/直传文件/不跳转/不导入/URL 转发/队列暂存）
- **对准判定**：场景词覆盖率（共享词 ÷ 用户场景词）≥ 0.5 **且**高频场景词（原话出现 ≥2 次）全部命中 = 对准。实证：15:09 覆盖率 0.83（5/6）+ 高频词（资云集×3/打开方式×2）全命中 → 判对准 ✓
- 注意「跳转」带否定语义（用户「没有跳转」= bug 现象 vs commit「修复不跳转」= 目标）——同主题共享，计入共享词，但语义方向判定归 L2
- 阈值标注为**建议初值**：P0a1 用 §4 冻结样本校准，验收①-③ 不依赖此阈值（防为样本拟合的 Goodhart 风险）

**目标锚定适用范围收窄（Claude 1728 C2 补）**：目标锚定适用于**场景词可提取的 session**；无场景词（模糊指令/纯确认）的 session = **不可判定**——P1 全库扫描时**排除并单独计数**，不计入锚定命中/错位统计（统计口径显式化）。

**验收③同步（Claude 1730 C1 补，T6 修正的必然连带）**：T6 从「初始错位检出」改为「负样本验证（不误报）」后，**验收③目标锚定部分必须同步**——原文「01a013f3 已知错位被 L1 候选检出」已无错位可检（hindsight 推翻），改为「**01a013f3 已知对准样本（15:09 vs 4b41ba8）不被误报为错位**」；空壳构建部分不变（10:25 正样本被规则命中）。**回写正文时验收③（SPEC §5 P0a 行 + 本方案 §1 P0a1b 行）随 T6 同步修订**——否则执行者按旧验收③会卡。

**C2 执行细节（Claude 1730 C2 记录）**：高频场景词「原话 ≥2 次」的计数粒度 = **单条 user 消息内重复**（实证：15:09 单条消息内资云集×3/打开方式×2），非跨消息累计；T6 实现时按此口径。

**验收③语义精确化（Claude 1733 C1/C2 补）**：
- **存在性 vs 有效性**：验收③的目标锚定部分 = **存在性验证**（检测点能跑、15:09 vs 4b41ba8 不被误报为错位）；**有效性验证**（能检出真实错位）**未验证**——依赖 P1 全库扫描找到正样本。回写正文时显式标注，防执行者误以为验收③证明了目标锚定有效。
- **待验证候选检测点**：目标锚定 = **待验证候选**，生命周期由数据决定——若 P1 全库扫描（形态 5-10）仍找不到「用户报 A、agent 首 commit 做 B」样本，**目标锚定删除**（不保留空洞定义），按 §9.6 数据反馈驱动。

### 9.4 hindsight 系统性检查（Claude 1717 C2，已示范）

**检查项（入 P0a1a 第 0 步）**：所有「行为定性」回溯到原始用户原话，不依赖事后解读。
- 示范①：`01a013f3`「两张图片」→ 实际出自注入日志（G8），「目标错位」→ 实际是深层需求点破（8/20）
- 示范②：`019ff89b`「自以为是」→ 实际是复刻对齐失败（16:02 报告），首条消息与首 commit 对准
- 检查范围：§4.6 样本定性（「需求对准」「多轮对准✗」）、§2.1 目标锚定实证、T6 验收口径——全部回溯原始 user 消息后再定

### 9.5 v1.4 修订记录（2026-08-20，冻结后执行就绪核查，Claude 1708-1721 四轮）

| # | Challenge | 判定 | 修订 |
|---|---|---|---|
| 1 | （1708 C2）收敛判据只覆盖设计层不覆盖执行就绪度——冻结过早 | **成立** | P0a1a 第 0 步 = 执行就绪核查正式化（9.1） |
| 2 | （1717 C1）T6 修正需三选项决策：改语义/换样本/检测点去留——先查 019ff89b | **实证排除选项③** | 019ff89b 首条消息 vs 首 commit 精确对准；改「样本不足」标注（9.3） |
| 3 | （1717 C2）hindsight bias 需系统性检查 | **成立** | 9.4 检查项 + 两样本示范 |
| 4 | （1721 C1）「伪检测点」归纳基于 2 个 bug 报告样本不能推广到模糊指令 | **成立（方法论）** | 改标「样本不足」；模糊指令类输入前提不满足（无场景词 → 不可判定） |
| 5 | （1721 C2）T6 去留需明确落地形态 | **成立** | T6 = 方向性报告（负样本验证，9.3-3） |
| 6 | （1721 C3）需求对齐并入 T8 的粒度边界 | **成立** | 标注跨子任务粒度 + Episode 切分依赖（P1）（9.3-2） |

| 7 | （1728 C1）负样本验证的「对准阈值」未定义——「期望输出对准」是断言不是验证 | **成立（实质）** | 补对准判定阈值：覆盖率 ≥0.5 + 高频场景词全命中（实证 0.83）；标注建议初值 + Goodhart 边界（9.3） |
| 8 | （1728 C2）「无场景词不可判定」收窄适用范围——统计口径需标注 | **成立** | 目标锚定适用于场景词可提取的 session，不可判定 = 排除 + 单独计数（9.3） |
| 9 | （1728 C3）回写正文 = 修改冻结内容——需明确「第二次冻结」语义 | **待用户决策** | 修订范围 = G1-G8 + T6 修正（定向修订，其余冻结内容不变）；版本语义：修正版冻结 vs 二次冻结由用户确认 |

| 10 | （1730 C1）验收③与 T6 修正必须同步——「目标锚定检出」变「不误报」后验收③语义变了 | **成立（连带）** | 验收③目标锚定部分改「01a013f3 已知对准不被误报为错位」；空壳构建不变；回写正文时 SPEC §5 + 方案 §1 同步（9.3） |
| 11 | （1730 C2）高频场景词「原话 ≥2 次」计数粒度未定 | 记录 | 粒度 = 单条 user 消息内重复（实证 15:09 资云集×3/打开方式×2），T6 实现按此口径 |

| 12 | （1733 C1）验收③改「不误报」后需标注只验存在性不验有效性 | **成立** | 验收③目标锚定 = 存在性验证（能跑、不误报）；有效性验证依赖 P1 正样本，显式标注（9.3） |
| 13 | （1733 C2）目标锚定是待验证候选——P1 无正样本应删除 | **成立** | 目标锚定标「待验证候选，P1 扫描后决定保留/删除」（9.3） |

| 14 | （1735 收敛确认）Claude 宣布停止挑战：语义精确化级别已达，继续挑战违反防表演原则 | 接受 | 收敛成立（实质问题数 = 0）；G6 决策预判补入（固定边界，BuildTurns 扩展留 P1）——进入用户拍板 |





**已回写（2026-08-20 用户确认修正版冻结）**：正文回写完成（SPEC §1/§2.1/§4.6/§5/§9.1 + 方案 §1/§2 T6/§3.1-3.3），两文档状态行已更新 → 进入 P0a1a 执行（第 0 步执行就绪核查 → T1-T5）。

---

## 10. P0a1a 执行记录（2026-08-24，codex 第 0 步 + T1-T5 落地）

> 执行纪律（§9）：checkpoint 输出客观对照物；G1-G8 逐项核对以代码/数据实证为准。

### 10.1 第 0 步：执行就绪核查 G1-G8 核对结果（全部完成）

| # | 缺口 | 核对方式 | 结果 |
|---|---|---|---|
| G1 | `normalizeToolName` mcp 大类过粗 | 代码核查 `eval/parse.go:305` | ✅ 已修：`classifyMcp` 细分 `mcp_aipm_search/trace/get/list/read/other`（T4 定向边界依赖） |
| G2 | `classifyCommand` 未接 aipm 工具 | 代码核查 `eval/extract.go:132` | ✅ 已修：`classifyAipmTool` 接入 CmdSemantics（aipm_search/trace/get/list/read/other） |
| G3 | `aipmc eval process` CLI 不存在 | `main.go` eval 仅 attribution | ✅ 已加：`--kind process --session <id> [--fix-hash] [--db]`（双输出复用） |
| G4 | ED 库无 `sessions` 表 | `.tables` 实测仅 `session_summaries` | ✅ 数据源修正；T1 start 从 `discussion_log` 首条 user 消息直接算 |
| G5 | `events` 表无分钟级事件边界 | 6/23-25 仅 2 条 `task_created` | ✅ §4.1 边界 = `C0ad2534FrozenEvents` 冻结常量表硬编码 |
| G6 | `isFakeUser` 过窄 | 代码核查 `eval/turn.go:102` | ✅ 按预判：P0a1a 固定边界不重切分；BuildTurns 扩展留 P1 |
| G7 | T6 首条消息对 01a013f3 不成立 | 规格实证（跨 3 天多子任务） | ✅ T6 归 P0a1b 负样本验证；Episode 复用标注 |
| G8 | 「两张图片」场景词引用错误 | hindsight 修正已回写正文 | ✅ 文档层已闭环 |

### 10.2 T1-T5 落地（c0ad2534 真实数据对照物）

**T1 时段边界**：start=2026-06-23 13:51:53（冻结表精确命中 ✓），end=2026-06-24 11:48:14（d628b7a ✓），休眠 17:38→08:58 跨夜标记 ✓。

**T2 d628b7a 关联**：`commit-20260624-114814-521602`，bug.commit_id 为空 → fallback=partial（标题关键词命中 4 个）→ `bug-20260624-114337-813aa9`，weak=false。与收工备注实证一致 ✓。

**T3 反馈识别**：介入=107，**纠偏=25（冻结 ground truth 精确命中 ✓，T3 独立验收召回=100%）**；存疑=0、注入排除=0（c0ad2534 为 6 月数据，无 019f 注入通道，符合预期）。

**T4 检索三分类**：自发=2（冻结口径 2 条真实自发 ✓）、被动=9、例行=0、比=0.22——**此为 mcp_tool 分型补盲前数值（仅 git 检索维度）；补盲后（§10.7 C1）：自发=2/被动=46/例行=1/比 0.04**（36 条 aipm 检索入管道，35 条落纠偏窗口；git show HEAD~1 修复后再 +1 被动，§10.7）。⚠️ 与 §4.1 表 0.05（43 被动）差异 = 口径不同（v1.4 定向边界 = git 历史 + aipm search/trace；§4.1 表为 v1.2 宽口径），方向性一致（被动主导），同口径下可比较。

**T5 死循环候选对照物（§4.1 冻结小时表映射）**：

| 小时 | frozen 判定 | L1 结果 | 差异记录（§9.6 数据反馈） |
|---|---|---|---|
| 6/23 15h | 正样本（盲试 build=20） | near-miss（build=16 自发=0，桶内有 edit） | **排除规则过严待校准**——盲试期"猜测补丁"edit 不应排除，需区分根因定位后修复（11h）vs 猜测修改（15h） |
| 6/23 16h | 正样本（盲试 build=15） | near-miss（build=10 自发=0，桶内有 edit） | 同上 |
| 6/24 09h | 正样本（盲试 build=17） | 未出候选 | **build 密集口径差异**——现数据 09h 无构建命令（grep/sed/git show 为主），frozen 17 与现数据不符，待复核 |
| 6/24 11h | 负样本（修复验证期） | 未误报 ✓ | 正确排除（build 计数未达阈值 + edit/commit 信号） |

### 10.3 待办（下次继续点）

1. ~~**T5 排除规则校准**~~ **已完成（2026-08-24，见 §10.5）**——排除规则参数化：edit/根因文本默认不排除（16h/15h 实证）、commit 仍排除（11h 实证）、被动检索>0 即非盲试（17h/10h 实证）。
2. ~~**09h build 口径复核**~~ **已完成（2026-08-24，见 §10.7）**——调查证据补齐：09:00-09:11 活跃段**零构建命令**（git show 二进制提取 ×2 + 文件读取 + 09:10:48 根因分析长文 + 09:11:46 纠偏后 8 次定向检索），整小时仅 1 条 objdump、27 次 edit；frozen build=17 **不可复现**（各桶 frozen 数字系统性高于现数据约 2 倍，疑似冻结后数据变更）。现数据语义 = 重分析阶段（非盲试），**edit 密集备选信号不采纳**（会把分析阶段误报为死循环）。
3. **T6-T9**（P0a1b）：目标锚定负样本验证（15:09 vs 4b41ba8）+ 空壳构建 + 验收报告。
4. **验收①-③ 跑通**：T9 验收报告需要 T5 校准后重算召回/误报。


### 10.4 Claude 审核修复（2026-08-24 第二轮，disc-20260824-103406）

Claude 审核 commit `4929bd8` 后指出 3 个规格偏差，均已修复：

| # | 偏差 | 修复 |
|---|---|---|
| 1 | T2 fallback 第 3 级 time_window 空实现（`pq_boundary.go` 循环体无判定） | ✅ 实现：commit 在 bug 创建 ±1h 且 files 有交集 → `time_window`；无交集 → `none`（弱 ground truth）。新增 `parseFilesJSON`/`splitFiles`/`filesIntersect` |
| 2 | T3 规则⑤「现代通道交 P1 L2」分支缺失（无 session 时代参数，一律计入介入） | ✅ `RecognizeFeedback(turns, modern)`：modern（019f 时代，`modernChannelSince`=6/26）→ ⑤ 输出介入候选清单（`ManualCandidates`）不直接计数；legacy 维持原口径 |
| 3 | T5 组合信号缺 失败/用户高峰/根因定位 | ✅ `DeadloopCandidate` 增 `Fails`/`UserMsgs` 记录项；排除规则补根因定位文本信号（`根因`/`已定位`/`原因已确认`） |

**修复后真实数据验证（c0ad2534）**：T3 纠偏=25 不变；T5 桶内信号完整输出（15h build=16 fail=0 user=8 根因文本排除、16h build=10 user=13 edit 排除）；11h 仍不误报。15h 根因文本（15:01:55「根因已确认」）为**错误根因声明**（用户 15:09 推回）——L1 无法区分正确/猜测根因，排除规则过严的校准张力仍在，对照物如实标注，T5 校准待办不变。

### 10.5 T5 排除规则校准结论（2026-08-24 第三轮，数据反馈驱动）

**校准输入**：c0ad2534 真实数据桶级信号（8/24 全量输出）vs §4.1 冻结小时表。

| 小时 | frozen 判定 | L1 信号（build/fail/user/edit/根因/自发/被动） | 校准前 | 校准后 |
|---|---|---|---|---|
| 6/23 15h | 正样本（盲试） | 16/0/8/3/1/0/0 | near-miss（edit+根因排除） | **候选命中 ✓** |
| 6/23 16h | 正样本（盲试） | 10/0/13/11/0/0/0 | near-miss（edit 排除） | **候选命中 ✓** |
| 6/24 09h | 正样本（09:00-09:11） | 无 build 命令 | 未出候选 | 未出候选（**数据差异**：frozen build=17 不可复现） |
| 6/24 10h | 负样本（纠偏响应） | 被动=18 | 未误报 | 未误报 ✓（被动>0） |
| 6/24 11h | 负样本（修复验证期） | commit 11:48 | 未误报 | 未误报 ✓（commit 排除） |

**校准决策（参数化，可回退）**：
1. **edit 不再排除**——16h 有 11 次 edit 仍是冻结正样本（猜测补丁 ≠ 修复进展）；`DeadloopParams.ExcludeEdit=false`
2. **根因文本不再排除**——15h 的 15:01:55「根因已确认」是错误根因声明（用户 15:09 推回）；L1 无法区分正确/猜测根因；`ExcludeRootCause=false`（信号仍记录输出）
3. **commit 仍排除**——11h 修复验证期靠桶内 fix commit（11:48）排除；`commit → excluded`
4. **被动>0 即非盲试**——17h/10h 纠偏响应（被动=18）实证；`PassiveMax=0`
5. **扫描上界 = fix commit 时刻**——之后的新问题时段（11:50:08 KSN bug）不属于本问题域

**验收①对照（build 可检出小时口径）**：正样本召回 = 2/2（15h/16h），负样本误报 = 0（10h/11h）。09h 为数据差异待复核。

**规格修订（2026-08-24 已回写）**：§2.1 排除规则原文「中间无 edit/commit/根因定位信号」按数据反馈（§9.6）校准为「默认仅 commit 排除 + 被动≤0」，已回写 `PROCESS_QUALITY_SPEC.md` §2.1 重复停滞行；参数保留原始规则开关（ExcludeEdit/ExcludeRootCause，默认 false）。

### 10.6 P0a1b 执行记录（2026-08-24，T6-T9 落地）

**T6 目标锚定**（`eval/pq_anchoring.go`）：
- 实现：场景词提取（单条消息内重复 ≥2 次的 CJK 子串，去停用词）+ 声称对象提取（首个 commit 标题问题描述部分六词分法）+ 对准判定（覆盖率 ≥0.5 且高频词全命中）
- 负样本验证（验收③存在性）：15:09 原话 vs 4b41ba8 实测 **覆盖率 5/6 = 0.83 + 高频词全命中 → 对准**（不误报）✓；`TestAnchoringNegativeSampleAligned`
- 声称对象词 = 第三方/打开方式/直传/文件/不跳转/不导入（直传文件按名词核心拆分，`nounCoreSuffix`）；否定前缀剥离（不跳转→跳转）计入共享，方向判定归 L2
- 口径记录：规格实证「资云集×3/打开方式×2 全命中」中资云集为产品专名（commit 以功能描述指代）——**已回写规格 §2.1（§10.8，Claude 审核 8/24 第二轮）**：高频词全命中 = 每个功能高频词必须命中 + 产品专名豁免；覆盖率分母 = 声称对象词（规格文字「用户场景词」为 0.67 口径，与实证 0.83 不等价——两口径均 ≥0.5 结论不变，公式以声称对象词为准）
- 无场景词（纯英文/无重复主题词）→ `Undecidable`（模糊指令类排除，单独计数）

**T7 构建产物完整性**（`eval/pq_artifact.go`）：
- 实现：构建命令（`isRealBuild`，T5 复用）后 30min 窗口内产物检查（ls/plutil/PlistBuddy/file/find 指向 `*.app`/`*.appex`）→ 空壳判定（ls total 极小且无主可执行 / CFBundleExecutable 占位符残留）
- 单样本检出（验收③）：01a013f3 8/20 10:24-10:27 空壳构建 **2 个候选命中**（10:26:38 占位符 + 10:27:26 ls total 16 空壳）✓；`TestHollowBuildDetected`
- 修复两处实现缺口：① `isRealBuild` 不识别 `xcodebuild ... build`（build 动词在参数后）——已补（T5/T7 共同受益）；② `DetectHollowBuilds` 只扫同 turn——10:26:21 user 贴安装失败日志切新 turn，改扁平时间索引跨 turn 扫描；③ `isArtifactCheck` 误用 T5 `buildNoisePathRe` 排除 DerivedData/Build 路径（产物恰在该目录下）——改只排除 Logs/Build + build 版本号

**T8 命令级五子信号**（`eval/pq_signal.go` + `pq_feedback.go` Referents）：
- 实现：响应（纠偏后 20min 内新行为，非检索非旧命令重复）/ 持续近似（响应后旧命令不重现）/ 收敛（用户确认词或修复 commit，**不绑 agent 自报**——「编译通过」不算）/ 对准近似（响应对象 vs 反馈所指，L1 领域映射 Share→分享）
- 反馈所指实体 = T3 提取输出（`FeedbackCandidate.Referents`，L1 正则候选；精确判定归 L2 matched_object P1）
- 真实数据（c0ad2534）：107 反馈事件，响应 103 / 持续 71 / 收敛 104 / 对准 0（L1 保守，英文响应对象与中文反馈所指无字面共享 → 全部标 ✗ 交 L2，符合 L1 低精度高召回设计）
- T8 = 报告输出（方向性），不占验收①-③（v1.3）

**T9 验收报告**（`eval/pq_report.go` + `aipmc eval acceptance`）：
- 聚合 T1-T5（process）+ T6-T8 → 验收①-③ 数据结果表（JSON + 人类可读）
- 真实数据全跑通：
  - 验收①：死循环召回 **120/131 分钟 = 92% ≥80%** ✅；负样本误报 **0 分钟 ≤15** ✅
  - 验收②：自发/被动比方向性（directional）+ 同项目对照（c0ad2534 vs 01a013f3 规格实证 48.6 倍）
  - 验收③：空壳构建单样本 **2 候选命中** ✅；目标锚定负样本 **对准（0.83）不误报** ✅
- CLI：`aipmc eval acceptance --session <id> [--fix-hash <prefix>] [--hollow-session <id>] [--anchor-msg <msg>] [--claim <title>]`
- 顺手修复：`aipmc eval process/acceptance` 位置参数 kind 未解析（usage 与实际不符）——已补位置参数解析（与 `--kind` 等价）

**遗留待确认（已处理，见 §10.8）**：① T8 对准近似 L1 英文-中文映射表（Share→分享等）为最小领域词表，P1 接入 L2 精确判定（P0a1 范围外）；② 验收① 09h 数据差异（frozen build=17 不可复现）已单独分账标注；③ T6 覆盖率分母口径已回写规格 §2.1。

### 10.7 Claude 第三轮 challenge 修复（2026-08-24，disc-20260824-115651-e652da）

Claude 深度审核 commit `4929bd8`（数据级实证：真实库逐命令核对 + ParseToolRecord 格式追踪），4 个实质挑战 + 9 个中小问题。本轮修复：

| # | 挑战/问题 | 修复 |
|---|---|---|
| C1 | **G1/G2 在验收样本上零覆盖**——真实 mcp 记录是 `{"type":"mcp_tool","tool":"aipm_smart_search"}` 格式，`ParseToolRecord` 无 mcp_tool 分型全落 unknown，T4 只统计了 git 检索（aipm 36 条在管道消失） | ✅ `parse.go` 补 `parseMcpTool`（type=mcp_tool → `classifyMcp` 归 mcp_aipm_search/trace/get/list/read/other）。**T4 重算：自发=2/被动=46/例行=1/比 0.04**（原 2/9——被动从 9 增至 46：36 条 aipm 检索入管道（35 条落纠偏窗口）+ git show HEAD~1 修复后 +1）。方向性不变（被动主导） |
| C2 | T5「build 密集」口径漂移——objdump/nm 反汇编、build-ios 路径操作被算进 build 密集 | 第 3 轮已参数化校准（§10.5）；本轮规格回写完成（`PROCESS_QUALITY_SPEC.md` §2.1） |
| C3 | frozen 表矛盾归因不一致——09h 没调查就归因 frozen 表错 | ✅ 调查证据补齐：09:00-09:11 零构建命令（见 §10.3 待办 2），frozen build=17 不可复现（各桶系统性偏高约 2 倍）；现数据语义 = 重分析阶段，edit 密集备选信号不采纳 |
| C4 | T2 Evidence 文案误导（显示前 N 个关键词）+ 平局无契约 | ✅ `keywordHits` 返回实际命中关键词（证据 = `palv2/header/sizeof/ble`，不再含 fix/跨平台兼容）；平局契约 = created_at 更早者优先（原报优先，数据值不随重建漂移），仍平局按 id 字典序 |
| 10 | 孤立回合（无 user 前置）`classifyUserText("")` → ⑤ → 介入计数污染 | ✅ `RecognizeFeedback` 跳过空 UserMsg 回合 |
| 11 | `git show HEAD~1`/`HEAD^` 被 `contains("git show head")` 子串误排除 | ✅ `gitShowCurrentState`：HEAD/HEAD:path = 当前态；HEAD~1/^/@{} = 历史检索 |
| 12 | `TestFindDeadloops` 只断言 excluded=1，未断言正样本 `Excluded=false` | ✅ 补按小时断言 15h/16h 正样本必须 Excluded=false |
| 13 | `commitTitleKeywords` 注释声称 2-gram 实现无；`hasCJK` 声称日韩文只覆盖汉字区 | ✅ 注释改为与实际实现一致（整词子串匹配）；`hasCJK` 扩平假名/片假名/谚文区 |

**T4 对照物更新**：自发=2/被动=46（原 9）——此前 T4 对照物在残缺数据上算出（Claude C1），重算后方向性结论不变但数值真实化。`TestParseMcpTool` 用真实验收格式断言（c0ad2534 38 条 mcp_tool 全覆盖）。

### 10.8 Claude 第二轮新挑战修复（2026-08-24，disc-20260824-122832-3508fc）

Claude 审核 HEAD=061f314 确认上轮 13 个 challenge 全部闭环（执行质量显著进步），但 P0a1b（T6-T9）带来 5 个新挑战。本轮全部处理：

| # | 挑战 | 修复 |
|---|---|---|
| 1 | **T6「高频词全命中」被稀释为「任一互含」**——15:09 高频词 资云集×3/打开×2 中资云集零覆盖，HighFreqAll=true 仅因打开↔打开方式互含；规格 8/20 冻结「资云集×3 全命中」断言本身是事实错误（4b41ba8 声称词无资云集） | ✅ `highFreqAllHit`：**每个功能高频词必须命中**（原任一互含），**产品专名豁免**（资云集 = dzsec 品牌专名，commit 以功能描述指代不要求字面重复）——规则不再为单样本调参；专名仍输出 SceneWords 供人工核对 |
| 2 | **T6 覆盖率分母矛盾（0.67 vs 0.83）**——规格文字「÷用户场景词」，实现「÷声称对象词」，§10.6「同值表述」数学上不成立 | ✅ 回写规格 §2.1：覆盖率 = 共享声称对象词 ÷ **声称对象词**（测声称被用户原话支撑的比例，与实证 0.83 一致）；用户场景词口径 0.67 也 ≥0.5 结论不变，双口径记录 |
| 3 | **验收①「120/131」= 小时桶覆盖**，09h 的 11 分钟被 92% 掩盖 | ✅ `pq_report.go` 增 `accept1DataDiff` 窗口：09:00-09:11 单独分账，detail 输出「可复现正样本召回 = 120/120 = 100%」+ 数据差异说明 |
| 4 | **验收②「48.6 倍」未实算**（复述规格文字） | ✅ 实时计算：`retrievalForSessionIfExists` 跑 01a013f3（自发=61/被动=48/比 1.27）vs c0ad2534（2/46/0.04）→ **29 倍 ≥5 倍 pass**（48.6 为 mcp_tool 补盲前旧数，已不适用）；对照 session 缺失时降级为方向性记录 |
| 5 | **T8 收敛=104/107 是「反馈早于修复 commit」的必然、对准=0 无信息量**（单 commit 样本） | ✅ `SignalReport.Notes` 信息量标注：收敛全部由 commitAfter 成立（无用户确认词增量证据）、对准 L1 保守全交 P1 L2——如实标注子信号无判别信息，不冒充有输出 |

**验收①-③ 真实数据重跑**：验收① 92%（可复现 120/120=100%）✅、误报 0 ✅、验收② 29 倍 ✅、验收③ 空壳 2 候选 + 目标锚定对准（0.83，功能高频词打开×2 全命中 + 资云集专名豁免）✅。测试全绿（仅既有 TestBuildAttributionFixture 日期硬编码失败）。

### 10.9 Claude 第三轮审核残留（2026-08-24，disc-20260824-130320-bc9069）

Claude 确认 873aab0 修复全部闭环、无立即需修代码缺陷，2 个残留记录为待办：

| # | 残留 | 处理 |
|---|---|---|
| 1 | **frozen 表系统性偏差根因未查明**——15h（20→16）、16h（15→10）、09h（17→0）三个冻结数字全部与现数据不符（约 2 倍系统性偏高）；09h 已调查（现数据为重分析阶段），但 8/19 冻结分析时的原始口径未追溯 | **待办（P0a1b 收尾后）**：回查冻结分析原始记录，给出归因（数据源不同？口径含检索/反汇编？），否则验收①-③「冻结 ground truth」可信度留有尾巴 |
| 2 | **productProperNoun 豁免表 = 硬编码资云集单样本**——「规则不再为单样本调参」声称不完整，豁免表本身是单样本产物 | 风险已被规格「待验证候选检测点，P1 扫描后决定保留/删除」承认，非缺陷；**P1 时需启发式识别产品名**（非查表） |

**顺手修复**：`TestBuildAttributionFixture` 日期硬编码（bug-20260824-101846 已 resolved）——fixture 全部改相对日期基准（base = now-2d，fixtureLog 与 Test 共用 ts() 助手），8 天前 orphan 排除用例保留。**eval 测试套件全绿**（此前唯一已知失败已消除）。
