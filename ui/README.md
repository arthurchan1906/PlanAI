Local React + Vite frontend for PMAI.

Run in dev mode:
- `cd ui`
- `npm run dev`

Build:
- `cd ui`
- `npm run build`

The built frontend is emitted to `src/pmai/ui/dist/`, which is the directory served by the Python web server.
