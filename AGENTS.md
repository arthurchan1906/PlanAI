Before coding, run `aipmc start`.
Use the current PMAI task/doc context before editing files.
Before creating new docs/tasks, run `aipmc search "<topic>"`.
At the end, run `aipmc session close --from-commits --from-tasks`.
